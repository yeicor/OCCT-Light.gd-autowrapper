<?xml version="1.0" encoding="UTF-8" ?>
<class name="OcctlCurves2d" inherits="RefCounted" version="4.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
		Wrapper for occtl_curves2d.h.
	</brief_description>
	<description>
		Auto-generated wrapper for the OCCT-Light C header occtl_curves2d.h.
	</description>
	<tutorials>
	</tutorials>
	<methods>
		<method name="const_OCCTL_CURVES2D_H">
			<return type="int64_t" />
			<description>
				@file occtl_curves2d.h @brief OCCT-Light: 2D curve (pcurve) API on BRepGraph. 2D curves are stored as graph representations (#occtl_rep_id_t with kind #OCCTL_REP_KIND_CURVE2D) on nodes of an #occtl_graph_t.  Every function in this header takes a graph pointer as its first parameter and identifies curves by representation ID. Kept in its own header so topology consumers that need only pcurves can include this without dragging in the 3D curve API. /.
			</description>
		</method>
		<method name="const_OCCTL_CURVE2D_CIRCLE_TANGENT_TO_TWO_RADIUS_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE2D_CIRCLE_TANGENT_TO_TWO_RADIUS_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE2D_BLEND_ARC_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE2D_BLEND_ARC_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE2D_LINE_TANGENT_TO_TWO_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE2D_LINE_TANGENT_TO_TWO_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE2D_LINE_TANGENT_THROUGH_POINT_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE2D_LINE_TANGENT_THROUGH_POINT_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE2D_LINE_TANGENT_WITH_ANGLE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE2D_LINE_TANGENT_WITH_ANGLE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE2D_CIRCLE_TANGENT_TO_THREE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE2D_CIRCLE_TANGENT_TO_THREE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE2D_CIRCLE_TANGENT_FIXED_CENTER_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE2D_CIRCLE_TANGENT_FIXED_CENTER_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE2D_CIRCLE_TANGENT_CENTER_ON_CURVE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE2D_CIRCLE_TANGENT_CENTER_ON_CURVE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE2D_CIRCLE_TANGENT_ON_CURVE_RADIUS_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE2D_CIRCLE_TANGENT_ON_CURVE_RADIUS_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE2D_BSPLINE_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE2D_BSPLINE_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE2D_BEZIER_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE2D_BEZIER_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE2D_OFFSET_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE2D_OFFSET_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE2D_BSPLINE_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE2D_BSPLINE_INIT.
			</description>
		</method>
		<method name="circle_tangent_to_two_radius_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurve2dCircleTangentToTwoRadiusInfo>" />
			<description>
				Runtime initialiser for #occtl_curve2d_circle_tangent_to_two_radius_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve2d_create_tangent_circle_to_two_radius /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="blend_arc_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurve2dBlendArcInfo>" />
			<description>
				Runtime initialiser for #occtl_curve2d_blend_arc_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve2d_create_blend_arc /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="line_tangent_to_two_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurve2dLineTangentToTwoInfo>" />
			<description>
				Runtime initialiser for #occtl_curve2d_line_tangent_to_two_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve2d_create_tangent_line_to_two /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="line_tangent_through_point_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurve2dLineTangentThroughPointInfo>" />
			<description>
				Runtime initialiser for #occtl_curve2d_line_tangent_through_point_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve2d_create_tangent_line_through_point /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="line_tangent_with_angle_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurve2dLineTangentWithAngleInfo>" />
			<description>
				Runtime initialiser for #occtl_curve2d_line_tangent_with_angle_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve2d_create_tangent_line_with_angle /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="circle_tangent_to_three_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurve2dCircleTangentToThreeInfo>" />
			<description>
				Runtime initialiser for #occtl_curve2d_circle_tangent_to_three_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve2d_create_tangent_circle_to_three /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="circle_tangent_fixed_center_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurve2dCircleTangentFixedCenterInfo>" />
			<description>
				Runtime initialiser for #occtl_curve2d_circle_tangent_fixed_center_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve2d_create_tangent_circle_fixed_center /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="circle_tangent_center_on_curve_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurve2dCircleTangentCenterOnCurveInfo>" />
			<description>
				Runtime initialiser for #occtl_curve2d_circle_tangent_center_on_curve_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve2d_create_tangent_circle_center_on_curve /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="circle_tangent_on_curve_radius_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurve2dCircleTangentOnCurveRadiusInfo>" />
			<description>
				Runtime initialiser for #occtl_curve2d_circle_tangent_on_curve_radius_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve2d_create_tangent_circle_on_curve_radius /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_tangent_circle_to_two_radius">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurve2dCircleTangentToTwoRadiusInfo>" />
			<argument index="2" name="solution_index" type="int" />
			<argument index="3" name="out_circle" type="Ref<OcctlGeom2dCircle>" />
			<argument index="4" name="out_count" type="int" />
			<description>
				Computes all fixed-radius 2D circles tangent to two input 2D curves. The results are returned one at a time by zero-based solution index.  Pass @p out_circle as NULL to read only the solution count, then request each solution by index. @param[in]  graph           Must be non-NULL. @param[in]  info            Borrows it.  Versioned solver inputs; must be non-NULL. @param[in]  solution_index  Zero-based solution index to read when @p out_circle is non-NULL. @param[out] out_circle      Borrows it.  Caller-owned slot, or NULL for count-only query. @param[out] out_count       Borrows it.  Receives the total solution count; must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p info or @p out_count is NULL, required fields are invalid, or @c p_next is non-NULL. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the tangent construction. @retval OCCTL_NOT_FOUND        @p out_circle is non-NULL and @p solution_index is outside the available solution range. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe Yes. @sa occtl_curve2d_create_circle, occtl_curve2d_circle_tangent_to_two_radius_info_init /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurve2dCircleTangentToTwoRadiusInfo]. Pass null to use defaults.
				    [param solution_index] Input parameter.
				    [param out_circle] Output parameter.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_blend_arc">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurve2dBlendArcInfo>" />
			<argument index="2" name="solution_index" type="int" />
			<argument index="3" name="out_id" type="int64_t" />
			<argument index="4" name="out_count" type="int" />
			<description>
				Computes fixed-radius 2D blend arcs tangent to two input 2D curves. The results are returned one at a time by zero-based solution index.  Pass @p out_id as NULL to read only the solution count, then request each solution by index.  Each returned curve is a trimmed 2D circle spanning the OCCT-computed tangency points. @param[in]  graph           Must be non-NULL. @param[in]  info            Borrows it.  Versioned solver inputs; must be non-NULL. @param[in]  solution_index  Zero-based solution index to read when @p out_id is non-NULL. @param[out] out_id          Borrows it.  Caller-allocated slot for a curve rep id, or NULL for count-only query. @param[out] out_count       Borrows it.  Receives the total solution count; must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p info or @p out_count is NULL, required fields are invalid, @c p_next is non-NULL, or the 0/1 flag is invalid. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the tangent construction. @retval OCCTL_NOT_FOUND        @p out_id is non-NULL and @p solution_index is outside the available solution range. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe Yes. @sa occtl_curve2d_blend_arc_info_init /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurve2dBlendArcInfo]. Pass null to use defaults.
				    [param solution_index] Input parameter.
				    [param out_id] Output parameter.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_tangent_line_to_two">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurve2dLineTangentToTwoInfo>" />
			<argument index="2" name="solution_index" type="int" />
			<argument index="3" name="out_line" type="Ref<OcctlGeom2dLine>" />
			<argument index="4" name="out_count" type="int" />
			<description>
				Computes all 2D lines tangent to two input 2D curves. The results are returned one at a time by zero-based solution index.  Pass @p out_line as NULL to read only the solution count, then request each solution by index. @param[in]  graph           Must be non-NULL. @param[in]  info            Borrows it.  Versioned solver inputs; must be non-NULL. @param[in]  solution_index  Zero-based solution index to read when @p out_line is non-NULL. @param[out] out_line        Borrows it.  Caller-owned slot, or NULL for count-only query. @param[out] out_count       Borrows it.  Receives the total solution count; must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p info or @p out_count is NULL, required fields are invalid, or @c p_next is non-NULL. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the tangent construction. @retval OCCTL_NOT_FOUND        @p out_line is non-NULL and @p solution_index is outside the available solution range. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe Yes. @sa occtl_curve2d_create_line, occtl_curve2d_line_tangent_to_two_info_init /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurve2dLineTangentToTwoInfo]. Pass null to use defaults.
				    [param solution_index] Input parameter.
				    [param out_line] Output parameter.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_tangent_line_through_point">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurve2dLineTangentThroughPointInfo>" />
			<argument index="2" name="solution_index" type="int" />
			<argument index="3" name="out_line" type="Ref<OcctlGeom2dLine>" />
			<argument index="4" name="out_count" type="int" />
			<description>
				Computes all 2D lines tangent to a curve and passing through a point. The results are returned one at a time by zero-based solution index.  Pass @p out_line as NULL to read only the solution count, then request each solution by index. @param[in]  graph           Must be non-NULL. @param[in]  info            Borrows it.  Versioned solver inputs; must be non-NULL. @param[in]  solution_index  Zero-based solution index to read when @p out_line is non-NULL. @param[out] out_line        Borrows it.  Caller-owned slot, or NULL for count-only query. @param[out] out_count       Borrows it.  Receives the total solution count; must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p info or @p out_count is NULL, required fields are invalid, or @c p_next is non-NULL. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the tangent construction. @retval OCCTL_NOT_FOUND        @p out_line is non-NULL and @p solution_index is outside the available solution range. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe Yes. @sa occtl_curve2d_create_line, occtl_curve2d_line_tangent_through_point_info_init /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurve2dLineTangentThroughPointInfo]. Pass null to use defaults.
				    [param solution_index] Input parameter.
				    [param out_line] Output parameter.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_tangent_line_with_angle">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurve2dLineTangentWithAngleInfo>" />
			<argument index="2" name="solution_index" type="int" />
			<argument index="3" name="out_line" type="Ref<OcctlGeom2dLine>" />
			<argument index="4" name="out_count" type="int" />
			<description>
				Computes all 2D lines tangent to a curve at a fixed angle to a reference line. The results are returned one at a time by zero-based solution index.  Pass @p out_line as NULL to read only the solution count, then request each solution by index. @param[in]  graph           Must be non-NULL. @param[in]  info            Borrows it.  Versioned solver inputs; must be non-NULL. @param[in]  solution_index  Zero-based solution index to read when @p out_line is non-NULL. @param[out] out_line        Borrows it.  Caller-owned slot, or NULL for count-only query. @param[out] out_count       Borrows it.  Receives the total solution count; must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p info or @p out_count is NULL, required fields are invalid, @c p_next is non-NULL, or the 0/1 flag is invalid. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the tangent construction. @retval OCCTL_NOT_FOUND        @p out_line is non-NULL and @p solution_index is outside the available solution range. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe Yes. @sa occtl_curve2d_create_line, occtl_curve2d_line_tangent_with_angle_info_init /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurve2dLineTangentWithAngleInfo]. Pass null to use defaults.
				    [param solution_index] Input parameter.
				    [param out_line] Output parameter.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_tangent_circle_to_three">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurve2dCircleTangentToThreeInfo>" />
			<argument index="2" name="solution_index" type="int" />
			<argument index="3" name="out_circle" type="Ref<OcctlGeom2dCircle>" />
			<argument index="4" name="out_count" type="int" />
			<description>
				Computes all 2D circles tangent to three input 2D curves. The results are returned one at a time by zero-based solution index.  Pass @p out_circle as NULL to read only the solution count, then request each solution by index. @param[in]  graph           Must be non-NULL. @param[in]  info            Borrows it.  Versioned solver inputs; must be non-NULL. @param[in]  solution_index  Zero-based solution index to read when @p out_circle is non-NULL. @param[out] out_circle      Borrows it.  Caller-owned slot, or NULL for count-only query. @param[out] out_count       Borrows it.  Receives the total solution count; must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p info or @p out_count is NULL, required fields are invalid, or @c p_next is non-NULL. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the tangent construction. @retval OCCTL_NOT_FOUND        @p out_circle is non-NULL and @p solution_index is outside the available solution range. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe Yes. @sa occtl_curve2d_create_circle, occtl_curve2d_circle_tangent_to_three_info_init /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurve2dCircleTangentToThreeInfo]. Pass null to use defaults.
				    [param solution_index] Input parameter.
				    [param out_circle] Output parameter.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_tangent_circle_fixed_center">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurve2dCircleTangentFixedCenterInfo>" />
			<argument index="2" name="solution_index" type="int" />
			<argument index="3" name="out_circle" type="Ref<OcctlGeom2dCircle>" />
			<argument index="4" name="out_count" type="int" />
			<description>
				Computes all 2D circles tangent to a curve with a fixed center point. The results are returned one at a time by zero-based solution index.  Pass @p out_circle as NULL to read only the solution count, then request each solution by index. @param[in]  graph           Must be non-NULL. @param[in]  info            Borrows it.  Versioned solver inputs; must be non-NULL. @param[in]  solution_index  Zero-based solution index to read when @p out_circle is non-NULL. @param[out] out_circle      Borrows it.  Caller-owned slot, or NULL for count-only query. @param[out] out_count       Borrows it.  Receives the total solution count; must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p info or @p out_count is NULL, required fields are invalid, or @c p_next is non-NULL. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the tangent construction. @retval OCCTL_NOT_FOUND        @p out_circle is non-NULL and @p solution_index is outside the available solution range. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe Yes. @sa occtl_curve2d_create_circle, occtl_curve2d_circle_tangent_fixed_center_info_init /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurve2dCircleTangentFixedCenterInfo]. Pass null to use defaults.
				    [param solution_index] Input parameter.
				    [param out_circle] Output parameter.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_tangent_circle_center_on_curve">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurve2dCircleTangentCenterOnCurveInfo>" />
			<argument index="2" name="solution_index" type="int" />
			<argument index="3" name="out_circle" type="Ref<OcctlGeom2dCircle>" />
			<argument index="4" name="out_count" type="int" />
			<description>
				Computes all 2D circles tangent to two curves with centers constrained to a curve. The results are returned one at a time by zero-based solution index.  Pass @p out_circle as NULL to read only the solution count, then request each solution by index. @param[in]  graph           Must be non-NULL. @param[in]  info            Borrows it.  Versioned solver inputs; must be non-NULL. @param[in]  solution_index  Zero-based solution index to read when @p out_circle is non-NULL. @param[out] out_circle      Borrows it.  Caller-owned slot, or NULL for count-only query. @param[out] out_count       Borrows it.  Receives the total solution count; must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p info or @p out_count is NULL, required fields are invalid, or @c p_next is non-NULL. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the tangent construction. @retval OCCTL_NOT_FOUND        @p out_circle is non-NULL and @p solution_index is outside the available solution range. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe Yes. @sa occtl_curve2d_create_circle, occtl_curve2d_circle_tangent_center_on_curve_info_init /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurve2dCircleTangentCenterOnCurveInfo]. Pass null to use defaults.
				    [param solution_index] Input parameter.
				    [param out_circle] Output parameter.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_tangent_circle_on_curve_radius">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurve2dCircleTangentOnCurveRadiusInfo>" />
			<argument index="2" name="solution_index" type="int" />
			<argument index="3" name="out_circle" type="Ref<OcctlGeom2dCircle>" />
			<argument index="4" name="out_count" type="int" />
			<description>
				Computes fixed-radius 2D circles tangent to one curve with centers constrained to another curve. The results are returned one at a time by zero-based solution index.  Pass @p out_circle as NULL to read only the solution count, then request each solution by index. @param[in]  graph           Must be non-NULL. @param[in]  info            Borrows it.  Versioned solver inputs; must be non-NULL. @param[in]  solution_index  Zero-based solution index to read when @p out_circle is non-NULL. @param[out] out_circle      Borrows it.  Caller-owned slot, or NULL for count-only query. @param[out] out_count       Borrows it.  Receives the total solution count; must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p info or @p out_count is NULL, required fields are invalid, or @c p_next is non-NULL, or radius/tolerance is invalid. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the tangent construction. @retval OCCTL_NOT_FOUND        @p out_circle is non-NULL and @p solution_index is outside the available solution range. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe Yes. @sa occtl_curve2d_create_circle, occtl_curve2d_circle_tangent_on_curve_radius_info_init /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurve2dCircleTangentOnCurveRadiusInfo]. Pass null to use defaults.
				    [param solution_index] Input parameter.
				    [param out_circle] Output parameter.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_line">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="line" type="Ref<OcctlGeom2dLine>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a 2D curve representation from a line. @param[in]  graph   Must be non-NULL. @param[in]  line    2D line data. @param[out] out_id  Borrows it.  Caller-allocated slot for the curve rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param line] A [OcctlGeom2dLine].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_circle">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="circle" type="Ref<OcctlGeom2dCircle>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a 2D curve representation from a circle. @param[in]  graph   Must be non-NULL. @param[in]  circle  2D circle data.  @c radius must be > 0. @param[out] out_id  Borrows it.  Caller-allocated slot for the curve rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID @c radius is not positive. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param circle] A [OcctlGeom2dCircle].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_ellipse">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="ellipse" type="Ref<OcctlGeom2dEllipse>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a 2D curve representation from an ellipse. @param[in]  graph    Must be non-NULL. @param[in]  ellipse  2D ellipse data. @param[out] out_id   Borrows it.  Caller-allocated slot for the curve rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID Radii invalid. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param ellipse] A [OcctlGeom2dEllipse].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_hyperbola">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="hyperbola" type="Ref<OcctlGeom2dHyperbola>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a 2D curve representation from a hyperbola. @param[in]  graph      Must be non-NULL. @param[in]  hyperbola  2D hyperbola data. @param[out] out_id     Borrows it.  Caller-allocated slot for the curve rep id. Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID Radii invalid. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param hyperbola] A [OcctlGeom2dHyperbola].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_parabola">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="parabola" type="Ref<OcctlGeom2dParabola>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a 2D curve representation from a parabola. @param[in]  graph     Must be non-NULL. @param[in]  parabola  2D parabola data.  @c focal_length must be > 0. @param[out] out_id    Borrows it.  Caller-allocated slot for the curve rep id. Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID @c focal_length is not positive. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param parabola] A [OcctlGeom2dParabola].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurve2dBsplineCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_curve2d_bspline_create_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve2d_create_bspline /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_bspline">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurve2dBsplineCreateInfo>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a 2D curve representation from a B-spline definition. @param[in]  graph   Must be non-NULL. @param[out] out_id  Borrows it.  Caller-allocated slot for the curve rep id. Must be non-NULL. @param[in]  info    Versioned create-info.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id or @p info is NULL. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the definition. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurve2dBsplineCreateInfo]. Pass null to use defaults.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bezier_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurve2dBezierCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_curve2d_bezier_create_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve2d_create_bezier /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_bezier">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurve2dBezierCreateInfo>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a 2D curve representation from a Bezier definition. @param[in]  graph   Must be non-NULL. @param[out] out_id  Borrows it.  Caller-allocated slot for the curve rep id. Must be non-NULL. @param[in]  info    Versioned create-info.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id or @p info is NULL. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the definition. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurve2dBezierCreateInfo]. Pass null to use defaults.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_trimmed">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurveTrimmedCreateInfo>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a trimmed 2D curve representation. @param[in]  graph   Must be non-NULL. @param[out] out_id  Borrows it.  Caller-allocated slot for the curve rep id. Must be non-NULL. @param[in]  info    Versioned create-info.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id or @p info is NULL. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID Invalid interval. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurveTrimmedCreateInfo]. Pass null to use defaults.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="offset_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurve2dOffsetCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_curve2d_offset_create_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve2d_create_offset /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_offset">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurve2dOffsetCreateInfo>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates an offset 2D curve representation. @param[in]  graph   Must be non-NULL. @param[out] out_id  Borrows it.  Caller-allocated slot for the curve rep id. Must be non-NULL. @param[in]  info    Versioned create-info.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id or @p info is NULL. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the offset. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurve2dOffsetCreateInfo]. Pass null to use defaults.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="reverse">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a new 2D curve representation that is the reversed copy of @p curve_id. The returned rep owns an independent curve whose parameterisation runs in the opposite direction. @param[in]  graph    Must be non-NULL. @param[in]  curve_id Curve to reverse. @param[out] out_id   Borrows it.  Caller-allocated slot for the reversed curve rep id. Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="transformed">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="translate_x" type="double" />
			<argument index="3" name="translate_y" type="double" />
			<argument index="4" name="rotate_angle" type="double" />
			<argument index="5" name="scale_x" type="double" />
			<argument index="6" name="scale_y" type="double" />
			<argument index="7" name="out_id" type="int64_t" />
			<description>
				Creates a new 2D curve representation by applying a 2D transform to @p curve_id. The transform is applied in the 2D parametric plane via @c gp_Trsf2d. @param[in]  graph            Must be non-NULL. @param[in]  curve_id         Curve to transform. @param[in]  translate_x      Translation along X. @param[in]  translate_y      Translation along Y. @param[in]  rotate_angle     Rotation angle in radians. @param[in]  scale_x          Scale factor along X. @param[in]  scale_y          Scale factor along Y. @param[out] out_id           Borrows it.  Caller-allocated slot for the transformed curve rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param translate_x] Input parameter.
				    [param translate_y] Input parameter.
				    [param rotate_angle] Input parameter.
				    [param scale_x] Input parameter.
				    [param scale_y] Input parameter.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="translated">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="delta" type="Ref<OcctlVector2>" />
			<argument index="3" name="out_id" type="int64_t" />
			<description>
				Creates a new 2D curve representation by translating @p curve_id. @param[in]  graph       Must be non-NULL. @param[in]  curve_id    Curve to translate. @param[in]  delta       Translation vector in 2D. @param[out] out_id      Borrows it.  Caller-allocated slot for the translated curve rep id. Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param delta] A [OcctlVector2].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="rotated">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="angle" type="double" />
			<argument index="3" name="out_id" type="int64_t" />
			<description>
				Creates a new 2D curve representation by rotating @p curve_id about the origin. @param[in]  graph      Must be non-NULL. @param[in]  curve_id   Curve to rotate. @param[in]  angle      Rotation angle in radians. @param[out] out_id     Borrows it.  Caller-allocated slot for the rotated curve rep id. Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param angle] Input parameter.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="scaled">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="origin" type="Ref<OcctlPoint2>" />
			<argument index="3" name="factor" type="double" />
			<argument index="4" name="out_id" type="int64_t" />
			<description>
				Creates a new 2D curve representation by scaling @p curve_id about @p origin. @param[in]  graph      Must be non-NULL. @param[in]  curve_id   Curve to scale. @param[in]  origin     Origin point for scaling. @param[in]  factor     Scale factor; must be non-zero. @param[out] out_id     Borrows it.  Caller-allocated slot for the scaled curve rep id. Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID @c factor is zero. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param origin] A [OcctlPoint2].
				    [param factor] Input parameter.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="length">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_length" type="PackedFloat64Array" />
			<description>
				Computes the length of the 2D curve over its full parameter range. @param[in]  graph        Must be non-NULL. @param[in]  curve_id     Curve to measure. @param[out] out_length   Receives the curve length.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_length is NULL. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_length] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="project_point">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="point" type="Ref<OcctlPoint2>" />
			<argument index="3" name="out_param" type="PackedFloat64Array" />
			<argument index="4" name="out_distance" type="PackedFloat64Array" />
			<description>
				Projects a 2D point onto the curve and returns the closest parameter. @param[in]  graph         Must be non-NULL. @param[in]  curve_id      Curve to project onto. @param[in]  point         Point to project. @param[out] out_param     Receives the parameter of the closest point.  Must be non-NULL. @param[out] out_distance  Receives the shortest distance.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_param is NULL. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param point] A [OcctlPoint2].
				    [param out_param] Output parameter.
				    [param out_distance] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="parameter_of_point">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="point" type="Ref<OcctlPoint2>" />
			<argument index="3" name="out_param" type="PackedFloat64Array" />
			<description>
				Returns the parameter on the 2D curve nearest to a given point. Convenience function equivalent to #occtl_curve2d_project_point with @c out_distance = NULL. @param[in]  graph      Must be non-NULL. @param[in]  curve_id   Curve to query. @param[in]  point      Point to find the parameter for. @param[out] out_param  Receives the parameter.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_param is NULL. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param point] A [OcctlPoint2].
				    [param out_param] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="kind">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_kind" type="int" />
			<description>
				Returns the kind of geometry of a 2D curve representation. @param[in]  graph     Must be non-NULL. @param[in]  curve_id  Curve to query. @param[out] out_kind  Borrows it.  Caller-allocated slot.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_kind is NULL. @threadsafe Yes (read-only on graph). @sa occtl_curve2d_as_circle, occtl_curve2d_as_ellipse /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_kind] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="is_periodic">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_is_periodic" type="PackedInt32Array" />
			<description>
				Returns non-zero if the 2D curve is periodic. @param[in]  graph        Must be non-NULL. @param[in]  curve_id     Curve to query. @param[out] out_is_periodic Borrows it.  Caller-allocated slot.  Must be non-NULL. 1 if periodic, 0 otherwise. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_is_periodic is NULL. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_is_periodic] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="is_closed">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_is_closed" type="PackedInt32Array" />
			<description>
				Returns non-zero if the 2D curve is closed. A 2D curve is closed when its start and end points coincide within OCCT's confusion tolerance.  Periodic curves are always closed; the converse does not hold. @param[in]  graph      Must be non-NULL. @param[in]  curve_id   Curve to query. @param[out] out_is_closed Borrows it.  Caller-allocated slot.  Must be non-NULL. 1 if closed, 0 otherwise. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_is_closed is NULL. @threadsafe Yes (read-only on graph). @sa occtl_curve2d_is_periodic /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_is_closed] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="continuity">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_continuity" type="int" />
			<description>
				Returns the continuity class of the 2D curve. @param[in]  graph           Must be non-NULL. @param[in]  curve_id        Curve to query. @param[out] out_continuity  Borrows it.  Caller-allocated slot.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_continuity is NULL. @threadsafe Yes (read-only on graph). /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_continuity] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="parameter_range">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_u_min" type="PackedFloat64Array" />
			<argument index="3" name="out_u_max" type="PackedFloat64Array" />
			<description>
				Returns the parameter range of a 2D curve. For analytic infinite curves (lines, parabolas, hyperbolas) OCCT returns @c -Precision::Infinite() and @c +Precision::Infinite() (numerically @c -1e100 and @c +1e100).  Bounded analytic curves (circles, ellipses) return [0, 2π]. @param[in]  graph       Must be non-NULL. @param[in]  curve_id    Curve to query. @param[out] out_u_min   Start parameter.  May be NULL. @param[out] out_u_max   End parameter.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_u_min] Output parameter.
				    [param out_u_max] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_line">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_line" type="Ref<OcctlGeom2dLine>" />
			<description>
				Extracts line data from a 2D curve representation. @param[in]  graph     Must be non-NULL. @param[in]  curve_id  Curve to extract; kind must be @c OCCTL_CURVE_KIND_LINE. @param[out] out_line  Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_line is NULL. @retval OCCTL_WRONG_KIND       Representation is not a line. @threadsafe Yes (read-only on graph). @sa occtl_curve2d_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_line] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_circle">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_circle" type="Ref<OcctlGeom2dCircle>" />
			<description>
				Extracts circle data from a 2D curve representation. @param[in]  graph       Must be non-NULL. @param[in]  curve_id    Curve to extract; kind must be @c OCCTL_CURVE_KIND_CIRCLE. @param[out] out_circle  Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_circle is NULL. @retval OCCTL_WRONG_KIND       Representation is not a circle. @threadsafe Yes (read-only on graph). @sa occtl_curve2d_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_circle] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_ellipse">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_ellipse" type="Ref<OcctlGeom2dEllipse>" />
			<description>
				Extracts ellipse data from a 2D curve representation. @param[in]  graph        Must be non-NULL. @param[in]  curve_id     Curve to extract; kind must be @c OCCTL_CURVE_KIND_ELLIPSE. @param[out] out_ellipse  Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_ellipse is NULL. @retval OCCTL_WRONG_KIND       Representation is not an ellipse. @threadsafe Yes (read-only on graph). @sa occtl_curve2d_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_ellipse] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_hyperbola">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_hyperbola" type="Ref<OcctlGeom2dHyperbola>" />
			<description>
				Extracts hyperbola data from a 2D curve representation. @param[in]  graph          Must be non-NULL. @param[in]  curve_id       Curve to extract; kind must be @c OCCTL_CURVE_KIND_HYPERBOLA. @param[out] out_hyperbola  Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_hyperbola is NULL. @retval OCCTL_WRONG_KIND       Representation is not a hyperbola. @threadsafe Yes (read-only on graph). @sa occtl_curve2d_kind /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_hyperbola] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_parabola">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_parabola" type="Ref<OcctlGeom2dParabola>" />
			<description>
				Extracts parabola data from a 2D curve representation. @param[in]  graph         Must be non-NULL. @param[in]  curve_id      Curve to extract; kind must be @c OCCTL_CURVE_KIND_PARABOLA. @param[out] out_parabola  Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_parabola is NULL. @retval OCCTL_WRONG_KIND       Representation is not a parabola. @threadsafe Yes (read-only on graph). @sa occtl_curve2d_kind /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_parabola] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_trimmed">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_u_first" type="PackedFloat64Array" />
			<argument index="3" name="out_u_last" type="PackedFloat64Array" />
			<description>
				Extracts the parameter bounds of a trimmed 2D curve. @param[in]  graph       Must be non-NULL. @param[in]  curve_id    Curve to extract; kind must be @c OCCTL_CURVE_KIND_TRIMMED. @param[out] out_u_first First parameter.  May be NULL. @param[out] out_u_last  Last parameter.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @retval OCCTL_WRONG_KIND       Representation is not a trimmed curve. @threadsafe Yes (read-only on graph). @sa occtl_curve2d_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_u_first] Output parameter.
				    [param out_u_last] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_offset">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_offset" type="PackedFloat64Array" />
			<description>
				Extracts the scalar offset of a 2D offset curve. @param[in]  graph       Must be non-NULL. @param[in]  curve_id    Curve to extract; kind must be @c OCCTL_CURVE_KIND_OFFSET. @param[out] out_offset  Signed offset distance.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @retval OCCTL_WRONG_KIND       Representation is not an offset curve. @threadsafe Yes (read-only on graph). @sa occtl_curve2d_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_offset] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_degree">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_degree" type="PackedInt32Array" />
			<description>
				Returns the polynomial degree of a 2D B-spline curve. @param[in]  graph       Must be non-NULL. @param[in]  curve_id    Curve to query; kind should be @c OCCTL_CURVE_KIND_BSPLINE. @param[out] out_degree  Borrows it.  Caller-allocated slot.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_degree is NULL. @retval OCCTL_WRONG_KIND       Not a B-spline. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_degree] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_pole_count">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_count" type="int" />
			<description>
				Returns the pole count of a 2D B-spline curve. @param[in]  graph       Must be non-NULL. @param[in]  curve_id    Curve to query. @param[out] out_count   Borrows it.  Caller-allocated slot.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Not a B-spline. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_knot_count">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_count" type="int" />
			<description>
				Returns the knot count of a 2D B-spline curve. @param[in]  graph       Must be non-NULL. @param[in]  curve_id    Curve to query. @param[out] out_count   Borrows it.  Caller-allocated slot.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Not a B-spline. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_is_rational">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_is_rational" type="PackedInt32Array" />
			<description>
				Returns non-zero if the 2D B-spline curve is rational. @param[in]  graph         Must be non-NULL. @param[in]  curve_id      Curve to query. @param[out] out_is_rational  Borrows it.  Caller-allocated slot.  Must be non-NULL. 1 if rational; 0 otherwise. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_is_rational is NULL. @retval OCCTL_WRONG_KIND       Not a B-spline. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_is_rational] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bezier_degree">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_degree" type="PackedInt32Array" />
			<description>
				Returns the polynomial degree of a 2D Bezier curve. Equivalent to @c pole_count - 1 but exposed for symmetry with #occtl_curve2d_bspline_degree. @param[in]  graph       Must be non-NULL. @param[in]  curve_id    Curve to query; kind must be @c OCCTL_CURVE_KIND_BEZIER. @param[out] out_degree  Borrows it.  Caller-allocated slot.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_degree is NULL. @retval OCCTL_WRONG_KIND       Not a Bezier curve. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_degree] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bezier_pole_count">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_count" type="int" />
			<description>
				Returns the pole count of a 2D Bezier curve. @param[in]  graph       Must be non-NULL. @param[in]  curve_id    Curve to query; kind must be @c OCCTL_CURVE_KIND_BEZIER. @param[out] out_count   Borrows it.  Caller-allocated slot.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Not a Bezier curve. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bezier_is_rational">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_is_rational" type="PackedInt32Array" />
			<description>
				Returns non-zero if the 2D Bezier curve is rational (has per-pole weights). @param[in]  graph         Must be non-NULL. @param[in]  curve_id      Curve to query; kind must be @c OCCTL_CURVE_KIND_BEZIER. @param[out] out_is_rational  Borrows it.  Caller-allocated slot.  Must be non-NULL. 1 if rational; 0 if non-rational or not a Bezier curve. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_is_rational is NULL. @retval OCCTL_WRONG_KIND       Not a Bezier curve. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_is_rational] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_poles">
			<return type="Array[Ref[OcctlPoint2]]" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<description>
				Extracts the poles of a 2D B-spline curve.  Two-call pattern. @param[in]  graph      Must be non-NULL. @param[in]  curve_id   Curve to query; kind must be @c OCCTL_CURVE_KIND_BSPLINE. @param[out] out_buf    May be NULL on sizing call. @param[in]  capacity   Elements @p out_buf can hold. @param[out] out_count  Required count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [return] Array[Ref[OcctlPoint2]] result.
		</description>
		</method>
		<method name="bspline_knots">
			<return type="PackedFloat64Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<description>
				Extracts the distinct knot values of a 2D B-spline curve.  Two-call pattern. @param[in]  graph      Must be non-NULL. @param[in]  curve_id   Curve to query. @param[out] out_buf    May be NULL on sizing call. @param[in]  capacity   Elements @p out_buf can hold. @param[out] out_count  Required count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [return] Array of double values.
		</description>
		</method>
		<method name="bspline_multiplicities">
			<return type="PackedInt32Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<description>
				Extracts the knot multiplicities of a 2D B-spline curve.  Two-call pattern. @param[in]  graph      Must be non-NULL. @param[in]  curve_id   Curve to query. @param[out] out_buf    May be NULL on sizing call. @param[in]  capacity   Elements @p out_buf can hold. @param[out] out_count  Required count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [return] Array of int values.
		</description>
		</method>
		<method name="bspline_weights">
			<return type="PackedFloat64Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<description>
				Extracts the per-pole weights of a rational 2D B-spline curve.  Two-call pattern. @param[in]  graph      Must be non-NULL. @param[in]  curve_id   Curve to query; must be rational. @param[out] out_buf    May be NULL on sizing call. @param[in]  capacity   Elements @p out_buf can hold. @param[out] out_count  Required count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Not a B-spline or not rational. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [return] Array of double values.
		</description>
		</method>
		<method name="bspline_flat_knots">
			<return type="PackedFloat64Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<description>
				Extracts the expanded (flat) knot sequence of a 2D B-spline curve.  Two-call pattern. @param[in]  graph      Must be non-NULL. @param[in]  curve_id   Curve to query. @param[out] out_buf    May be NULL on sizing call. @param[in]  capacity   Elements @p out_buf can hold. @param[out] out_count  Required count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes (read-only on graph). /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [return] Array of double values.
		</description>
		</method>
		<method name="bspline_poles_view">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_count" type="int" />
			<description>
				Zero-copy view of the poles array of a 2D B-spline curve. Returns a direct pointer into the OCCT internal storage.  Valid only while the underlying curve is alive and no mutating call has been made on it. @param[in]  graph      Must be non-NULL. @param[in]  curve_id   Curve to query; kind must be @c OCCTL_CURVE_KIND_BSPLINE. @param[out] out_data   Receives a borrowed pointer to the pole array.  Must be non-NULL. @param[out] out_count  Receives the pole count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_data, or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Representation is not a B-spline. @threadsafe Yes (read-only). /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_init">
			<argument index="0" name="out" type="Ref<OcctlCurve2dBspline>" />
			<description>
				Runtime initialiser for #occtl_curve2d_bspline_t. @param[out] out  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve2d_as_bspline /
				    [param out] Output parameter.
		</description>
		</method>
		<method name="as_bspline">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out" type="Ref<OcctlCurve2dBspline>" />
			<description>
				Fills a caller-allocated #occtl_curve2d_bspline_t with a complete read-only inspection view of the underlying 2D B-spline curve. Pointers in @p out borrow from the underlying curve and are valid until the representation is freed from the graph. @param[in]     graph    Must be non-NULL. @param[in]     curve_id Curve to inspect; kind must be @c OCCTL_CURVE_KIND_BSPLINE. @param[in,out] out      Borrows it.  Caller-allocated; must be non-NULL.  The caller sets @c struct_version on entry; the library fills the remaining fields on success and leaves them unchanged on failure. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out is NULL. @retval OCCTL_VERSION_MISMATCH @c out->struct_version is not supported. @retval OCCTL_WRONG_KIND       Representation is not a 2D B-spline curve. @threadsafe Yes (read-only). @sa occtl_curve2d_bspline_init, occtl_curve2d_bspline_poles_view, occtl_curve2d_bspline_degree, occtl_curve2d_bspline_pole_count /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="to_bezier_segments">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="options" type="Ref<OcctlCurveBezierSegmentsOptions>" />
			<argument index="3" name="out_count" type="int" />
			<description>
				Decomposes a 2D curve into adjacent Bezier curve segments. The implementation delegates conversion and segmentation to OCCT @c Geom2dConvert and @c Geom2dConvert_BSplineCurveToBezierCurve.  Each output entry is a new bezier curve rep id.  Free the returned array with #occtl_curve2d_free_bezier_segments. @param[in]  graph         Must be non-NULL. @param[in]  curve_id      Curve to decompose. @param[in]  options       Borrows it. May be NULL for default full-domain options. @param[out] out_segments  Owns it. Receives a caller-owned array of Bezier curve rep ids. Must be non-NULL. Free with #occtl_curve2d_free_bezier_segments. @param[out] out_count     Borrows it. Receives the number of returned segments. Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_segments, or @p out_count is NULL; @c p_next is non-NULL; a 0/1 flag is invalid; range/tolerance values are invalid. @retval OCCTL_VERSION_MISMATCH Unsupported @c options->struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT could not convert or segment the curve. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe Yes. @sa occtl_curve2d_create_bezier, occtl_curve_bezier_segments_options_init /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param options] An optional [OcctlCurveBezierSegmentsOptions]. Pass null to use defaults.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="free_bezier_segments">
			<argument index="0" name="ids" type="int64_t" />
			<description>
				Frees the array returned by #occtl_curve2d_to_bezier_segments. NULL-tolerant; passing NULL is a no-op. @param[in] ids  Owns it.  May be NULL. @threadsafe Yes. @sa occtl_curve2d_to_bezier_segments /
				    [param ids] Output parameter.
		</description>
		</method>
		<method name="eval_d0">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="out_point" type="Ref<OcctlPoint2>" />
			<description>
				Evaluates the 2D curve at parameter @p u. @param[in]  graph     Must be non-NULL. @param[in]  curve_id  Curve to evaluate. @param[in]  u         Parameter value. @param[out] out_point Receives the point on the curve.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @retval OCCTL_OUT_OF_RANGE     Parameter is outside the domain. @threadsafe Yes (read-only on graph). @sa occtl_curve2d_eval_d1, occtl_curve2d_eval_dn /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param out_point] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="eval_d1">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="out_point" type="Ref<OcctlPoint2>" />
			<argument index="4" name="out_d1" type="Ref<OcctlVector2>" />
			<description>
				Evaluates the 2D curve and its first derivative at @p u. @param[in]  graph     Must be non-NULL. @param[in]  curve_id  Curve to evaluate. @param[in]  u         Parameter value. @param[out] out_point Receives the point.  May be NULL. @param[out] out_d1    Receives the first derivative vector.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @retval OCCTL_OUT_OF_RANGE     Parameter is outside the domain. @threadsafe Yes (read-only on graph). @sa occtl_curve2d_eval_d0, occtl_curve2d_eval_d2 /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param out_point] Output parameter.
				    [param out_d1] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="eval_d2">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="out_point" type="Ref<OcctlPoint2>" />
			<argument index="4" name="out_d1" type="Ref<OcctlVector2>" />
			<argument index="5" name="out_d2" type="Ref<OcctlVector2>" />
			<description>
				Evaluates point, first, and second derivatives at @p u. @param[in]  graph     Must be non-NULL. @param[in]  curve_id  Curve to evaluate. @param[in]  u         Parameter value. @param[out] out_point Receives the point.  May be NULL. @param[out] out_d1    Receives the first derivative vector.  May be NULL. @param[out] out_d2    Receives the second derivative vector.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @retval OCCTL_OUT_OF_RANGE     Parameter is outside the domain. @threadsafe Yes (read-only on graph). @sa occtl_curve2d_eval_d1, occtl_curve2d_eval_d3 /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param out_point] Output parameter.
				    [param out_d1] Output parameter.
				    [param out_d2] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="eval_d3">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="out_point" type="Ref<OcctlPoint2>" />
			<argument index="4" name="out_d1" type="Ref<OcctlVector2>" />
			<argument index="5" name="out_d2" type="Ref<OcctlVector2>" />
			<argument index="6" name="out_d3" type="Ref<OcctlVector2>" />
			<description>
				Evaluates point and first three derivatives at @p u. @param[in]  graph     Must be non-NULL. @param[in]  curve_id  Curve to evaluate. @param[in]  u         Parameter value. @param[out] out_point Receives the point.  May be NULL. @param[out] out_d1    Receives the first derivative vector.  May be NULL. @param[out] out_d2    Receives the second derivative vector.  May be NULL. @param[out] out_d3    Receives the third derivative vector.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @retval OCCTL_OUT_OF_RANGE     Parameter is outside the domain. @threadsafe Yes (read-only on graph). @sa occtl_curve2d_eval_d2, occtl_curve2d_eval_dn /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param out_point] Output parameter.
				    [param out_d1] Output parameter.
				    [param out_d2] Output parameter.
				    [param out_d3] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="eval_dn">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="n" type="int" />
			<argument index="4" name="out_derivative" type="Ref<OcctlVector2>" />
			<description>
				Returns the N-th derivative vector of the 2D curve at @p u. @param[in]  graph          Must be non-NULL. @param[in]  curve_id       Curve to evaluate. @param[in]  u              Parameter value. @param[in]  n              Derivative order (0 returns a zero-length vector whose origin is on the curve; 1 returns the first derivative, etc.). @param[out] out_derivative Receives the derivative vector.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @retval OCCTL_OUT_OF_RANGE     Parameter is outside the domain. @threadsafe Yes (read-only on graph). @sa occtl_curve2d_eval_d0, occtl_curve2d_eval_d3 /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param n] Input parameter.
				    [param out_derivative] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
	</methods>
	<constants>
		<constant name="OCCTL_CURVE2D_CIRCLE_TANGENT_TO_TWO_RADIUS_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE2D_CIRCLE_TANGENT_TO_TWO_RADIUS_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE2D_BLEND_ARC_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE2D_BLEND_ARC_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE2D_LINE_TANGENT_TO_TWO_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE2D_LINE_TANGENT_TO_TWO_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE2D_LINE_TANGENT_THROUGH_POINT_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE2D_LINE_TANGENT_THROUGH_POINT_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE2D_LINE_TANGENT_WITH_ANGLE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE2D_LINE_TANGENT_WITH_ANGLE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE2D_CIRCLE_TANGENT_TO_THREE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE2D_CIRCLE_TANGENT_TO_THREE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE2D_CIRCLE_TANGENT_FIXED_CENTER_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE2D_CIRCLE_TANGENT_FIXED_CENTER_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE2D_CIRCLE_TANGENT_CENTER_ON_CURVE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE2D_CIRCLE_TANGENT_CENTER_ON_CURVE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE2D_CIRCLE_TANGENT_ON_CURVE_RADIUS_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE2D_CIRCLE_TANGENT_ON_CURVE_RADIUS_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE2D_BSPLINE_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE2D_BSPLINE_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE2D_BEZIER_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE2D_BEZIER_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE2D_OFFSET_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE2D_OFFSET_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE2D_BSPLINE_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE2D_BSPLINE_VERSION_1.
		</constant>
	</constants>
</class>
