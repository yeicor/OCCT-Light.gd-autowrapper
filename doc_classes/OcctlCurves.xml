<?xml version="1.0" encoding="UTF-8" ?>
<class name="OcctlCurves" inherits="RefCounted" version="4.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
		Wrapper for occtl_curves.h.
	</brief_description>
	<description>
		Auto-generated wrapper for the OCCT-Light C header occtl_curves.h.
	</description>
	<tutorials>
	</tutorials>
	<methods>
		<method name="const_OCCTL_CURVES_H">
			<return type="int64_t" />
			<description>
				@file occtl_curves.h @brief OCCT-Light: 3D curve representation in the topology graph. A curve is stored as an #occtl_rep_id_t inside an #occtl_graph_t.  Every curve function takes a graph pointer and a rep id.  The kind is available via #occtl_curve_kind for O(1) introspection. This file covers construction, introspection, data extraction, and parametric evaluation of 3D curve representations. /.
			</description>
		</method>
		<method name="const_OCCTL_CURVE_BSPLINE_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE_BSPLINE_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE_BEZIER_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE_BEZIER_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE_OFFSET_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE_OFFSET_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE_BSPLINE_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE_BSPLINE_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE_AIRFOIL_NACA4_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE_AIRFOIL_NACA4_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE_INTERPOLATED_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE_INTERPOLATED_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_CURVE_APPROXIMATED_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_CURVE_APPROXIMATED_INFO_INIT.
			</description>
		</method>
		<method name="create_line">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="line" type="Ref<OcctlAxis1Placement>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a 3D curve representation from a line. @param[in]  graph    Owning graph.  Must be non-NULL. @param[in]  line     Line data (origin + unit direction). @param[out] out_id   Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_curve_kind, occtl_curve_as_line /
				    [param graph] Input parameter.
				    [param line] A [OcctlAxis1Placement].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_circle">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="circle" type="Ref<OcctlGeomCircle>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a 3D curve representation from a circle. @param[in]  graph    Owning graph.  Must be non-NULL. @param[in]  circle   Circle data (frame + radius).  @c radius must be > 0. @param[out] out_id   Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID @c circle.radius is not positive. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_curve_as_circle /
				    [param graph] Input parameter.
				    [param circle] A [OcctlGeomCircle].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_ellipse">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="ellipse" type="Ref<OcctlGeomEllipse>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a 3D curve representation from an ellipse. @param[in]  graph    Owning graph.  Must be non-NULL. @param[in]  ellipse  Ellipse data.  @c major_radius >= @c minor_radius > 0. @param[out] out_id   Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID Radii are not positive or major < minor. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_curve_as_ellipse /
				    [param graph] Input parameter.
				    [param ellipse] A [OcctlGeomEllipse].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_hyperbola">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="hyperbola" type="Ref<OcctlGeomHyperbola>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a 3D curve representation from a hyperbola. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  hyperbola  Hyperbola data.  Both radii must be > 0. @param[out] out_id     Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID A radius is not positive. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_curve_as_hyperbola /
				    [param graph] Input parameter.
				    [param hyperbola] A [OcctlGeomHyperbola].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_parabola">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="parabola" type="Ref<OcctlGeomParabola>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a 3D curve representation from a parabola. @param[in]  graph    Owning graph.  Must be non-NULL. @param[in]  parabola Parabola data.  @c focal_length must be > 0. @param[out] out_id   Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID @c focal_length is not positive. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_curve_as_parabola /
				    [param graph] Input parameter.
				    [param parabola] A [OcctlGeomParabola].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurveBsplineCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_curve_bspline_create_info_t. Sets @c struct_version and zeroes all other fields.  Use instead of the @c _INIT macro when C macros are not available (e.g. C# P/Invoke). @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve_create_bspline /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_bspline">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurveBsplineCreateInfo>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a 3D curve representation from a B-spline definition. @param[in]  graph    Owning graph.  Must be non-NULL. @param[in]  info     Versioned create-info struct.  Must be non-NULL.  Arrays inside are borrowed; they need not remain valid after the call returns. @param[out] out_id   Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL; or required array fields are NULL. @retval OCCTL_VERSION_MISMATCH @c info->struct_version is not supported. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the knot/pole/degree combination. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_curve_bspline_degree, occtl_curve_bspline_poles /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurveBsplineCreateInfo]. Pass null to use defaults.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bezier_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurveBezierCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_curve_bezier_create_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve_create_bezier /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_bezier">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurveBezierCreateInfo>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a 3D curve representation from a Bezier definition. @param[in]  graph    Owning graph.  Must be non-NULL. @param[in]  info     Versioned create-info.  Must be non-NULL. @param[out] out_id   Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the pole count or weights. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurveBezierCreateInfo]. Pass null to use defaults.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_trimmed">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurveTrimmedCreateInfo>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a trimmed 3D curve (bounded section of a basis curve). @param[in]  graph    Owning graph.  Must be non-NULL. @param[in]  info     Versioned create-info.  Must be non-NULL. @param[out] out_id   Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL; or @c info->basis is invalid. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID @c u_last <= @c u_first, or OCCT rejected the interval. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurveTrimmedCreateInfo]. Pass null to use defaults.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="offset_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurveOffsetCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_curve_offset_create_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve_create_offset /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_offset">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurveOffsetCreateInfo>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates an offset 3D curve. @param[in]  graph    Owning graph.  Must be non-NULL. @param[in]  info     Versioned create-info.  Must be non-NULL. @param[out] out_id   Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL; or @c info->basis is invalid. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID @c offset_dir has zero length. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurveOffsetCreateInfo]. Pass null to use defaults.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="reverse">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a new curve representation that is the reversed copy of @p curve_id. The returned rep owns an independent curve whose parameterisation runs in the opposite direction: @c reversed(u) = @c curve(u_last - u). @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to reverse. @param[out] out_id     Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_curve_create_line /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="transformed">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="transform" type="Ref<OcctlTransform>" />
			<argument index="3" name="out_id" type="int64_t" />
			<description>
				Creates a new curve representation by applying a 3D transform to @p curve_id. The returned rep owns an independent transformed copy. @param[in]  graph           Owning graph.  Must be non-NULL. @param[in]  curve_id        Curve to transform. @param[in]  transform       Transform to apply. @param[out] out_id          Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_curve_create_line /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param transform] A [OcctlTransform].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="translated">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="delta" type="Ref<OcctlVector3>" />
			<argument index="3" name="out_id" type="int64_t" />
			<description>
				Creates a new curve representation by translating @p curve_id. Convenience wrapper equivalent to applying a translation transform. @param[in]  graph        Owning graph.  Must be non-NULL. @param[in]  curve_id     Curve to translate. @param[in]  delta        Translation vector. @param[out] out_id       Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_curve_create_line /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param delta] A [OcctlVector3].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="rotated">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="axis" type="Ref<OcctlAxis1Placement>" />
			<argument index="3" name="angle" type="double" />
			<argument index="4" name="out_id" type="int64_t" />
			<description>
				Creates a new curve representation by rotating @p curve_id. Convenience wrapper equivalent to applying a rotation transform. @param[in]  graph        Owning graph.  Must be non-NULL. @param[in]  curve_id     Curve to rotate. @param[in]  axis         Rotation axis (origin + direction). @param[in]  angle        Rotation angle in radians. @param[out] out_id       Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_curve_create_line /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param axis] A [OcctlAxis1Placement].
				    [param angle] Input parameter.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="scaled">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="origin" type="Ref<OcctlPoint3>" />
			<argument index="3" name="factor" type="double" />
			<argument index="4" name="out_id" type="int64_t" />
			<description>
				Creates a new curve representation by scaling @p curve_id about @p origin. Convenience wrapper equivalent to applying a non-uniform scale transform. @param[in]  graph        Owning graph.  Must be non-NULL. @param[in]  curve_id     Curve to scale. @param[in]  origin       Origin point for scaling. @param[in]  factor       Scale factor; must be non-zero. @param[out] out_id       Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID @c factor is zero. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_curve_create_line /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param origin] A [OcctlPoint3].
				    [param factor] Input parameter.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="length">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_length" type="PackedFloat64Array" />
			<description>
				Computes the length of the curve over its full parameter range. The length is computed by numerical integration using OCCT's @c GCPnts_AbscissaPoint. @param[in]  graph       Owning graph.  Must be non-NULL. @param[in]  curve_id    Curve to measure. @param[out] out_length  Receives the curve length.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_length is NULL. @threadsafe Only when distinct @p graph instances are used. @sa occtl_curve_create_line /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_length] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="project_point">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="point" type="Ref<OcctlPoint3>" />
			<argument index="3" name="out_param" type="PackedFloat64Array" />
			<argument index="4" name="out_distance" type="PackedFloat64Array" />
			<description>
				Projects a 3D point onto the curve and returns the closest parameter. Uses OCCT's @c GeomAPI_ProjectPointOnCurve.  When multiple solutions exist (e.g. a circle), the closest point is returned. @param[in]  graph         Owning graph.  Must be non-NULL. @param[in]  curve_id      Curve to project onto. @param[in]  point         Point to project. @param[out] out_param     Receives the parameter of the closest point on the curve. Must be non-NULL. @param[out] out_distance  Receives the shortest distance.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_param is NULL. @threadsafe Only when distinct @p graph instances are used. @sa occtl_curve_create_line /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param point] A [OcctlPoint3].
				    [param out_param] Output parameter.
				    [param out_distance] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="parameter_of_point">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="point" type="Ref<OcctlPoint3>" />
			<argument index="3" name="out_param" type="PackedFloat64Array" />
			<description>
				Returns the parameter on the curve nearest to a given 3D point. Convenience function equivalent to #occtl_curve_project_point with @c out_distance = NULL. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to query. @param[in]  point      Point to find the parameter for. @param[out] out_param  Receives the parameter.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_param is NULL. @threadsafe Only when distinct @p graph instances are used. @sa occtl_curve_create_line /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param point] A [OcctlPoint3].
				    [param out_param] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="kind">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_kind" type="int" />
			<description>
				Returns the kind of geometry wrapped by a curve representation. @param[in]  graph     Owning graph.  Must be non-NULL. @param[in]  curve_id  Curve to query. @param[out] out_kind  Receives the curve kind.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_kind is NULL. @threadsafe Yes. @sa occtl_curve_as_circle, occtl_curve_as_ellipse /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_kind] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="is_periodic">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_is_periodic" type="PackedInt32Array" />
			<description>
				Returns non-zero if the curve is periodic. @param[in]  graph            Owning graph.  Must be non-NULL. @param[in]  curve_id         Curve to query. @param[out] out_is_periodic  Receives 1 if periodic, 0 otherwise.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_is_periodic is NULL. @threadsafe Yes. @sa occtl_curve_create_line /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_is_periodic] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="is_closed">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_is_closed" type="PackedInt32Array" />
			<description>
				Returns non-zero if the curve is closed. A curve is closed when its start and end points coincide within OCCT's confusion tolerance.  Periodic curves are always closed; the converse does not hold. @param[in]  graph         Owning graph.  Must be non-NULL. @param[in]  curve_id      Curve to query. @param[out] out_is_closed Receives 1 if closed, 0 otherwise.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_is_closed is NULL. @threadsafe Yes. @sa occtl_curve_is_periodic /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_is_closed] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="continuity">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_continuity" type="int" />
			<description>
				Returns the continuity class of the curve. @param[in]  graph            Owning graph.  Must be non-NULL. @param[in]  curve_id         Curve to query. @param[out] out_continuity   Receives the continuity class.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_continuity is NULL. @threadsafe Yes. @sa occtl_curve_create_line /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_continuity] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="parameter_range">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_u_min" type="PackedFloat64Array" />
			<argument index="3" name="out_u_max" type="PackedFloat64Array" />
			<description>
				Returns the parameter range of the curve. For analytic infinite curves (lines, parabolas, hyperbolas) OCCT returns @c -Precision::Infinite() and @c +Precision::Infinite() (numerically @c -1e100 and @c +1e100).  Bounded analytic curves (full circles, ellipses) return [0, 2@c pi].  Trimmed and B-spline curves return their explicit range. @param[in]  graph       Owning graph.  Must be non-NULL. @param[in]  curve_id    Curve to query. @param[out] out_u_min   First (start) parameter.  May be NULL. @param[out] out_u_max   Last (end) parameter.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @threadsafe Yes. @sa occtl_curve_create_line /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_u_min] Output parameter.
				    [param out_u_max] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_line">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_line" type="Ref<OcctlAxis1Placement>" />
			<description>
				Extracts line data from a curve representation. @param[in]  graph     Owning graph.  Must be non-NULL. @param[in]  curve_id  Curve to extract; kind must be @c OCCTL_CURVE_KIND_LINE. @param[out] out_line  Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_line is NULL. @retval OCCTL_WRONG_KIND       Curve is not a line. @threadsafe Yes. @sa occtl_curve_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_line] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_circle">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_circle" type="Ref<OcctlGeomCircle>" />
			<description>
				Extracts circle data from a curve representation. @param[in]  graph       Owning graph.  Must be non-NULL. @param[in]  curve_id    Curve to extract; kind must be @c OCCTL_CURVE_KIND_CIRCLE. @param[out] out_circle  Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_circle is NULL. @retval OCCTL_WRONG_KIND       Curve is not a circle. @threadsafe Yes. @sa occtl_curve_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_circle] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_ellipse">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_ellipse" type="Ref<OcctlGeomEllipse>" />
			<description>
				Extracts ellipse data from a curve representation. @param[in]  graph        Owning graph.  Must be non-NULL. @param[in]  curve_id     Curve to extract; kind must be @c OCCTL_CURVE_KIND_ELLIPSE. @param[out] out_ellipse  Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_ellipse is NULL. @retval OCCTL_WRONG_KIND       Curve is not an ellipse. @threadsafe Yes. @sa occtl_curve_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_ellipse] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_hyperbola">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_hyperbola" type="Ref<OcctlGeomHyperbola>" />
			<description>
				Extracts hyperbola data from a curve representation. @param[in]  graph          Owning graph.  Must be non-NULL. @param[in]  curve_id       Curve to extract; kind must be @c OCCTL_CURVE_KIND_HYPERBOLA. @param[out] out_hyperbola  Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_hyperbola is NULL. @retval OCCTL_WRONG_KIND       Curve is not a hyperbola. @threadsafe Yes. @sa occtl_curve_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_hyperbola] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_parabola">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_parabola" type="Ref<OcctlGeomParabola>" />
			<description>
				Extracts parabola data from a curve representation. @param[in]  graph         Owning graph.  Must be non-NULL. @param[in]  curve_id      Curve to extract; kind must be @c OCCTL_CURVE_KIND_PARABOLA. @param[out] out_parabola  Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_parabola is NULL. @retval OCCTL_WRONG_KIND       Curve is not a parabola. @threadsafe Yes. @sa occtl_curve_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_parabola] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_trimmed">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_u_first" type="PackedFloat64Array" />
			<argument index="3" name="out_u_last" type="PackedFloat64Array" />
			<description>
				Extracts the parameter bounds of a trimmed curve. The reported @c u_first / @c u_last are the parameter bounds the trimmed curve was constructed with (already adjusted for any sense flip at construction time), so reconstructing via #occtl_curve_create_trimmed with @c sense=1 reproduces an equivalent curve. @param[in]  graph        Owning graph.  Must be non-NULL. @param[in]  curve_id     Curve to extract; kind must be @c OCCTL_CURVE_KIND_TRIMMED. @param[out] out_u_first  First parameter.  May be NULL. @param[out] out_u_last   Last parameter.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @retval OCCTL_WRONG_KIND       Curve is not a trimmed curve. @threadsafe Yes. @sa occtl_curve_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_u_first] Output parameter.
				    [param out_u_last] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_offset">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_offset" type="PackedFloat64Array" />
			<argument index="3" name="out_offset_dir" type="Ref<OcctlVector3>" />
			<description>
				Extracts the scalar offset and reference direction of an offset curve. @param[in]  graph           Owning graph.  Must be non-NULL. @param[in]  curve_id        Curve to extract; kind must be @c OCCTL_CURVE_KIND_OFFSET. @param[out] out_offset      Signed offset distance.  May be NULL. @param[out] out_offset_dir  Reference direction (unit length).  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @retval OCCTL_WRONG_KIND       Curve is not an offset curve. @threadsafe Yes. @sa occtl_curve_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_offset] Output parameter.
				    [param out_offset_dir] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_degree">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_degree" type="PackedInt32Array" />
			<description>
				Returns the polynomial degree of a B-spline curve. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to query; kind must be @c OCCTL_CURVE_KIND_BSPLINE. @param[out] out_degree Receives the degree.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_degree is NULL. @retval OCCTL_WRONG_KIND       Curve is not a B-spline. @threadsafe Yes. @sa occtl_curve_create_bspline /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_degree] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_pole_count">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_count" type="int" />
			<description>
				Returns the pole count of a B-spline curve. @param[in]  graph         Owning graph.  Must be non-NULL. @param[in]  curve_id      Curve to query; kind must be @c OCCTL_CURVE_KIND_BSPLINE. @param[out] out_count     Receives the pole count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Curve is not a B-spline. @threadsafe Yes. @sa occtl_curve_create_bspline /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_knot_count">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_count" type="int" />
			<description>
				Returns the knot count (number of distinct knot values) of a B-spline curve. @param[in]  graph         Owning graph.  Must be non-NULL. @param[in]  curve_id      Curve to query; kind must be @c OCCTL_CURVE_KIND_BSPLINE. @param[out] out_count     Receives the knot count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Curve is not a B-spline. @threadsafe Yes. @sa occtl_curve_create_bspline /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_is_rational">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_is_rational" type="PackedInt32Array" />
			<description>
				Returns non-zero if the B-spline curve is rational (has per-pole weights). @param[in]  graph            Owning graph.  Must be non-NULL. @param[in]  curve_id         Curve to query; kind must be @c OCCTL_CURVE_KIND_BSPLINE. @param[out] out_is_rational  Receives 1 if rational, 0 otherwise.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_is_rational is NULL. @retval OCCTL_WRONG_KIND       Curve is not a B-spline. @threadsafe Yes. @sa occtl_curve_create_bspline /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_is_rational] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bezier_degree">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_degree" type="PackedInt32Array" />
			<description>
				Returns the polynomial degree of a Bezier curve. Equivalent to @c pole_count - 1 but exposed for symmetry with #occtl_curve_bspline_degree. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to query; kind must be @c OCCTL_CURVE_KIND_BEZIER. @param[out] out_degree Receives the degree.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_degree is NULL. @retval OCCTL_WRONG_KIND       Curve is not a Bezier curve. @threadsafe Yes. @sa occtl_curve_create_bezier /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_degree] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bezier_pole_count">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_count" type="int" />
			<description>
				Returns the pole count of a Bezier curve. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to query; kind must be @c OCCTL_CURVE_KIND_BEZIER. @param[out] out_count  Receives the pole count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Curve is not a Bezier curve. @threadsafe Yes. @sa occtl_curve_create_bezier /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bezier_is_rational">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_is_rational" type="PackedInt32Array" />
			<description>
				Returns non-zero if the Bezier curve is rational (has per-pole weights). @param[in]  graph            Owning graph.  Must be non-NULL. @param[in]  curve_id         Curve to query; kind must be @c OCCTL_CURVE_KIND_BEZIER. @param[out] out_is_rational  Receives 1 if rational, 0 otherwise.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_is_rational is NULL. @retval OCCTL_WRONG_KIND       Curve is not a Bezier curve. @threadsafe Yes. @sa occtl_curve_create_bezier /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_is_rational] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_poles">
			<return type="Array[Ref[OcctlPoint3]]" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<description>
				Extracts the poles of a B-spline curve into a caller-supplied buffer. Two-call pattern: call with @p out_buf == NULL to learn the required count in @p out_count, allocate, then call again with a buffer of at least that size. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to extract; kind must be @c OCCTL_CURVE_KIND_BSPLINE. @param[out] out_buf    Buffer to receive pole data.  May be NULL on the sizing call. @param[in]  capacity   Number of elements @p out_buf can hold. @param[out] out_count  Required count.  Must be non-NULL. @retval OCCTL_OK               On success (poles written). @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Curve is not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL @p out_buf is non-NULL but @p capacity < *@p out_count. @threadsafe Yes. @sa occtl_curve_create_bspline /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [return] Array[Ref[OcctlPoint3]] result.
		</description>
		</method>
		<method name="bspline_knots">
			<return type="PackedFloat64Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<description>
				Extracts the distinct knot values of a B-spline curve. Two-call pattern: same as #occtl_curve_bspline_poles. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to extract; kind must be @c OCCTL_CURVE_KIND_BSPLINE. @param[out] out_buf    Buffer to receive knot values.  May be NULL on sizing call. @param[in]  capacity   Capacity of @p out_buf in elements. @param[out] out_count  Required count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Curve is not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes. @sa occtl_curve_create_bspline /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [return] Array of double values.
		</description>
		</method>
		<method name="bspline_multiplicities">
			<return type="PackedInt32Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<description>
				Extracts the knot multiplicities of a B-spline curve. Two-call pattern: same as #occtl_curve_bspline_poles. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to extract; kind must be @c OCCTL_CURVE_KIND_BSPLINE. @param[out] out_buf    Buffer to receive multiplicity values.  May be NULL on sizing call. @param[in]  capacity   Capacity of @p out_buf in elements. @param[out] out_count  Required count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Curve is not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes. @sa occtl_curve_create_bspline /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [return] Array of int values.
		</description>
		</method>
		<method name="bspline_weights">
			<return type="PackedFloat64Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<description>
				Extracts the per-pole weights of a rational B-spline curve. Returns @c OCCTL_WRONG_KIND if the curve is non-rational.  Check #occtl_curve_bspline_is_rational before calling this function. Two-call pattern: same as #occtl_curve_bspline_poles. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to extract; kind must be @c OCCTL_CURVE_KIND_BSPLINE; curve must be rational. @param[out] out_buf    Buffer to receive weight values.  May be NULL on sizing call. @param[in]  capacity   Capacity of @p out_buf in elements. @param[out] out_count  Required count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Curve is not a B-spline, or B-spline is non-rational. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes. @sa occtl_curve_create_bspline /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [return] Array of double values.
		</description>
		</method>
		<method name="bspline_poles_view">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out_count" type="int" />
			<description>
				Zero-copy view of the poles array of a B-spline curve. Returns a direct pointer into the OCCT internal storage.  Valid only while the graph rep is alive and no mutating call has been made on it. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to view; kind must be @c OCCTL_CURVE_KIND_BSPLINE. @param[out] out_data   Receives a borrowed pointer to the pole array.  Must be non-NULL. @param[out] out_count  Receives the pole count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_data, or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Curve is not a B-spline. @threadsafe Yes (read-only). @sa occtl_curve_create_bspline /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_flat_knots">
			<return type="PackedFloat64Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<description>
				Extracts the expanded (flat) knot sequence of a B-spline curve. The flat knot sequence repeats each distinct knot value by its multiplicity, which is the form most numerical libraries (NumPy, etc.) expect.  The total count equals the sum of multiplicities. Two-call pattern: same as #occtl_curve_bspline_poles. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to extract; kind must be @c OCCTL_CURVE_KIND_BSPLINE. @param[out] out_buf    Buffer to receive flat knot values.  May be NULL on sizing call. @param[in]  capacity   Capacity of @p out_buf in elements. @param[out] out_count  Required count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL. @retval OCCTL_WRONG_KIND       Curve is not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes. @sa occtl_curve_create_bspline /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [return] Array of double values.
		</description>
		</method>
		<method name="bspline_init">
			<argument index="0" name="out" type="Ref<OcctlCurveBspline>" />
			<description>
				Runtime initialiser for #occtl_curve_bspline_t. Sets @c struct_version and zeroes all other fields.  Use instead of the @c _INIT macro when C macros are not available (e.g. C# P/Invoke). @param[out] out  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve_as_bspline /
				    [param out] Output parameter.
		</description>
		</method>
		<method name="as_bspline">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="out" type="Ref<OcctlCurveBspline>" />
			<description>
				Fills a caller-allocated #occtl_curve_bspline_t with a complete read-only inspection view of the underlying B-spline curve. Pointers in @p out borrow from the graph rep and are valid until the rep is removed from the graph. @param[in]     graph    Owning graph.  Must be non-NULL. @param[in]     curve_id Curve to inspect; kind must be @c OCCTL_CURVE_KIND_BSPLINE. @param[in,out] out      Borrows it.  Caller-allocated; must be non-NULL.  The caller sets @c struct_version on entry; the library fills the remaining fields on success and leaves them unchanged on failure. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out is NULL. @retval OCCTL_VERSION_MISMATCH @c out->struct_version is not supported. @retval OCCTL_WRONG_KIND       Curve is not a B-spline curve. @threadsafe Yes (read-only). @sa occtl_curve_bspline_init, occtl_curve_bspline_poles_view, occtl_curve_bspline_degree, occtl_curve_bspline_pole_count /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param out] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="eval_d0">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="out_point" type="Ref<OcctlPoint3>" />
			<description>
				Evaluates the curve at parameter @p u. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to evaluate. @param[in]  u          Parameter value. @param[out] out_point  Receives the point on the curve.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @retval OCCTL_OUT_OF_RANGE     @p u is outside the parameter domain. @threadsafe Only when distinct @p graph instances are used. @sa occtl_curve_eval_d1, occtl_curve_eval_dn /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param out_point] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="eval_d1">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="out_point" type="Ref<OcctlPoint3>" />
			<argument index="4" name="out_d1" type="Ref<OcctlVector3>" />
			<description>
				Evaluates the curve and its first derivative at @p u. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to evaluate. @param[in]  u          Parameter value. @param[out] out_point  Receives the point.  May be NULL. @param[out] out_d1     Receives the first derivative vector.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @retval OCCTL_OUT_OF_RANGE     @p u is outside the parameter domain. @threadsafe Only when distinct @p graph instances are used. @sa occtl_curve_eval_d0, occtl_curve_eval_d2 /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param out_point] Output parameter.
				    [param out_d1] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="eval_d2">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="out_point" type="Ref<OcctlPoint3>" />
			<argument index="4" name="out_d1" type="Ref<OcctlVector3>" />
			<argument index="5" name="out_d2" type="Ref<OcctlVector3>" />
			<description>
				Evaluates point, first, and second derivatives at @p u. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to evaluate. @param[in]  u          Parameter value. @param[out] out_point  Receives the point.  May be NULL. @param[out] out_d1     Receives the first derivative vector.  May be NULL. @param[out] out_d2     Receives the second derivative vector.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @retval OCCTL_OUT_OF_RANGE     @p u is outside the parameter domain. @threadsafe Only when distinct @p graph instances are used. @sa occtl_curve_eval_d1, occtl_curve_eval_d3 /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param out_point] Output parameter.
				    [param out_d1] Output parameter.
				    [param out_d2] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="eval_d3">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="out_point" type="Ref<OcctlPoint3>" />
			<argument index="4" name="out_d1" type="Ref<OcctlVector3>" />
			<argument index="5" name="out_d2" type="Ref<OcctlVector3>" />
			<argument index="6" name="out_d3" type="Ref<OcctlVector3>" />
			<description>
				Evaluates point and first three derivatives at @p u. @param[in]  graph      Owning graph.  Must be non-NULL. @param[in]  curve_id   Curve to evaluate. @param[in]  u          Parameter value. @param[out] out_point  Receives the point.  May be NULL. @param[out] out_d1     Receives the first derivative vector.  May be NULL. @param[out] out_d2     Receives the second derivative vector.  May be NULL. @param[out] out_d3     Receives the third derivative vector.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @retval OCCTL_OUT_OF_RANGE     @p u is outside the parameter domain. @threadsafe Only when distinct @p graph instances are used. @sa occtl_curve_eval_d2, occtl_curve_eval_dn /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param out_point] Output parameter.
				    [param out_d1] Output parameter.
				    [param out_d2] Output parameter.
				    [param out_d3] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="eval_dn">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="n" type="int" />
			<argument index="4" name="out_derivative" type="Ref<OcctlVector3>" />
			<description>
				Returns the N-th derivative vector at @p u. @param[in]  graph           Owning graph.  Must be non-NULL. @param[in]  curve_id        Curve to evaluate. @param[in]  u               Parameter value. @param[in]  n               Derivative order (0 returns a zero-length vector whose origin is on the curve; 1 returns the first derivative, etc.). @param[out] out_derivative  Receives the derivative vector.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL. @retval OCCTL_OUT_OF_RANGE     @p u is outside the parameter domain. @threadsafe Only when distinct @p graph instances are used. @sa occtl_curve_eval_d0, occtl_curve_eval_d3 /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param curve_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param n] Input parameter.
				    [param out_derivative] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="airfoil_naca4_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurveAirfoilNaca4Info>" />
			<description>
				Runtime initialiser for #occtl_curve_airfoil_naca4_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve_create_airfoil_naca4 /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_airfoil_naca4">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurveAirfoilNaca4Info>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a B-spline curve approximating a NACA 4-digit airfoil profile. The wrapper generates cosine-spaced NACA profile samples and delegates curve fitting to OCCT's @c GeomAPI_PointsToBSpline.  For a standard NACA 2412 profile, set @c max_camber=0.02, @c camber_position=0.4, and @c thickness=0.12. @param[in]  graph    Owning graph.  Must be non-NULL. @param[in]  info     Borrows it. Versioned airfoil parameters; must be non-NULL. @param[out] out_id   Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL, @c p_next is non-NULL, a 0/1 flag is invalid, or a numeric parameter is outside its valid range. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the generated point fit. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_curve_airfoil_naca4_info_init, occtl_curve_create_approximated /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurveAirfoilNaca4Info]. Pass null to use defaults.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_arc_of_circle_3pt">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="p1" type="Ref<OcctlPoint3>" />
			<argument index="2" name="p2" type="Ref<OcctlPoint3>" />
			<argument index="3" name="p3" type="Ref<OcctlPoint3>" />
			<argument index="4" name="out_id" type="int64_t" />
			<description>
				Creates a trimmed circular arc through three 3D points. The arc runs from @p p1 through @p p2 to @p p3.  The three points must not be collinear and must be distinct. @param[in]  graph    Owning graph.  Must be non-NULL. @param[in]  p1       First point. @param[in]  p2       Second point (intermediate). @param[in]  p3       Third point. @param[out] out_id   Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK                On success. @retval OCCTL_INVALID_ARGUMENT  @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID  The three points are collinear or cannot define a unique circle. @threadsafe Yes. @sa occtl_curve_create_circle, occtl_curve_create_trimmed /
				    [param graph] Input parameter.
				    [param p1] A [OcctlPoint3].
				    [param p2] A [OcctlPoint3].
				    [param p3] A [OcctlPoint3].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="interpolated_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurveInterpolatedInfo>" />
			<description>
				Runtime initialiser for #occtl_curve_interpolated_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve_create_interpolated /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_interpolated">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurveInterpolatedInfo>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a B-spline curve that interpolates (passes through) the given points. Uses OCCT's @c GeomAPI_Interpolate.  The resulting curve is always a C2 continuous cubic B-spline.  Set @c is_periodic to make the curve periodic (first and last points must match within tolerance). @param[in]  graph    Owning graph.  Must be non-NULL. @param[in]  info     Versioned create-info.  Must be non-NULL. @param[out] out_id   Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL, or required fields are NULL / zero. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID Interpolation failed. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_curve_interpolated_info_init /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurveInterpolatedInfo]. Pass null to use defaults.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="approximated_info_init">
			<argument index="0" name="info" type="Ref<OcctlCurveApproximatedInfo>" />
			<description>
				Runtime initialiser for #occtl_curve_approximated_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_curve_create_approximated /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_approximated">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlCurveApproximatedInfo>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Fits a B-spline curve to the given points within tolerance. Uses OCCT's @c GeomAPI_PointsToBSpline.  The resulting curve approximates the points; it does not necessarily pass through them. @param[in]  graph    Owning graph.  Must be non-NULL. @param[in]  info     Versioned create-info.  Must be non-NULL. @param[out] out_id   Receives the new rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL, or required fields are NULL / zero. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID Approximation failed. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_curve_approximated_info_init /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlCurveApproximatedInfo]. Pass null to use defaults.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="to_bezier_segments">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id" type="int64_t" />
			<argument index="2" name="options" type="Ref<OcctlCurveBezierSegmentsOptions>" />
			<argument index="3" name="out_count" type="int" />
			<description>
				Decomposes a curve into adjacent Bezier curve segments. The implementation delegates conversion and segmentation to OCCT @c GeomConvert and @c GeomConvert_BSplineCurveToBezierCurve.  Each output entry is a new rep id of kind #OCCTL_CURVE_KIND_BEZIER.  Free the returned array with #occtl_curve_free_bezier_segments. @param[in]  graph        Owning graph.  Must be non-NULL. @param[in]  curve_id     Curve to decompose. @param[in]  options      Borrows it. May be NULL for default full-domain options. @param[out] out_ids      Owns it. Receives an allocated array of rep ids. Must be non-NULL.  Free with #occtl_curve_free_bezier_segments. @param[out] out_count    Borrows it. Receives the number of returned segments. Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_ids, or @p out_count is NULL; @c p_next is non-NULL; a 0/1 flag is invalid; range/tolerance values are invalid. @retval OCCTL_VERSION_MISMATCH Unsupported @c options->struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT could not convert or segment the curve. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe Yes. @sa occtl_curve_create_bezier, occtl_curve_bezier_segments_options_init /
				    [param graph] Input parameter.
				    [param curve_id] A occtl_rep_id_t.
				    [param options] An optional [OcctlCurveBezierSegmentsOptions]. Pass null to use defaults.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="free_bezier_segments">
			<argument index="0" name="ids" type="int64_t" />
			<description>
				Frees the array returned by #occtl_curve_to_bezier_segments. NULL-tolerant; passing NULL is a no-op. @param[in] ids  Owns it.  May be NULL. @threadsafe Yes. @sa occtl_curve_to_bezier_segments /
				    [param ids] Output parameter.
		</description>
		</method>
		<method name="intersect">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="curve_id_a" type="int64_t" />
			<argument index="2" name="curve_id_b" type="int64_t" />
			<argument index="3" name="out_count" type="int" />
			<description>
				Finds the closest points between two curves. Uses OCCT's @c GeomAPI_ExtremaCurveCurve.  Returns all extrema points (minima and maxima of distance) between the two curves.  When the curves intersect, the distance is zero and the returned points coincide. The caller owns the returned array and must free it with #occtl_curve_free_intersection_points. @param[in]  graph        Owning graph.  Must be non-NULL. @param[in]  curve_id_a   First curve. @param[in]  curve_id_b   Second curve. @param[out] out_results  Receives a caller-owned array of intersection points.  Must be non-NULL. Free with #occtl_curve_free_intersection_points. @param[out] out_count    Receives the number of intersection points. Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_results, or @p out_count is NULL. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe Yes. @sa occtl_curve_create_line /
				    [param graph] Input parameter.
				    [param curve_id_a] A occtl_rep_id_t.
				    [param curve_id_b] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="free_intersection_points">
			<argument index="0" name="results" type="Ref<OcctlCurveIntersectionPoint>" />
			<description>
				Frees the array returned by #occtl_curve_intersect. NULL-tolerant; passing NULL is a no-op. @param[in] results  Owns it.  May be NULL. @threadsafe Yes. @sa occtl_curve_intersect /
				    [param results] Output parameter.
		</description>
		</method>
	</methods>
	<constants>
		<constant name="OCCTL_CURVE_BSPLINE_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE_BSPLINE_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE_BEZIER_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE_BEZIER_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE_OFFSET_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE_OFFSET_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE_BSPLINE_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE_BSPLINE_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE_AIRFOIL_NACA4_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE_AIRFOIL_NACA4_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE_INTERPOLATED_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE_INTERPOLATED_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_CURVE_APPROXIMATED_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_CURVE_APPROXIMATED_INFO_VERSION_1.
		</constant>
	</constants>
</class>
