<?xml version="1.0" encoding="UTF-8" ?>
<class name="OcctlSurfaces" inherits="RefCounted" version="4.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
		Wrapper for occtl_surfaces.h.
	</brief_description>
	<description>
		Auto-generated wrapper for the OCCT-Light C header occtl_surfaces.h.
	</description>
	<tutorials>
	</tutorials>
	<methods>
		<method name="const_OCCTL_SURFACES_H">
			<return type="int64_t" />
			<description>
				@file occtl_surfaces.h @brief OCCT-Light: surface geometry via graph + rep_id. Surface geometry is accessed and created through the BRepGraph topology DAG using #occtl_rep_id_t identifiers.  Every surface function takes an #occtl_graph_t* as the first argument followed by the surface's rep id. Constructors and query functions write output rep ids into caller-provided slots.  The caller passes the graph that owns the surface representation; all rep ids are scoped to that graph instance. /.
			</description>
		</method>
		<method name="const_OCCTL_SURFACE_REVOLUTION_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_SURFACE_REVOLUTION_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_SURFACE_EXTRUSION_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_SURFACE_EXTRUSION_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_SURFACE_RECTANGULAR_TRIMMED_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_SURFACE_RECTANGULAR_TRIMMED_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_SURFACE_OFFSET_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_SURFACE_OFFSET_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_SURFACE_BSPLINE_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_SURFACE_BSPLINE_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_SURFACE_BEZIER_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_SURFACE_BEZIER_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_SURFACE_BSPLINE_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_SURFACE_BSPLINE_INIT.
			</description>
		</method>
		<method name="const_OCCTL_SURFACE_INTERPOLATED_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_SURFACE_INTERPOLATED_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_SURFACE_APPROXIMATED_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_SURFACE_APPROXIMATED_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_SURFACE_POINT_GRID_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_SURFACE_POINT_GRID_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_SURFACE_BOUNDARY_CURVES_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_SURFACE_BOUNDARY_CURVES_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_SURFACE_GORDON_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_SURFACE_GORDON_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="const_OCCTL_SURFACE_CURVE_GRID_CREATE_INFO_INIT">
			<return type="int64_t" />
			<description>
				Constant wrapper for #define OCCTL_SURFACE_CURVE_GRID_CREATE_INFO_INIT.
			</description>
		</method>
		<method name="create_plane">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="plane" type="Ref<OcctlGeomPlane>" />
			<description>
				Creates a plane surface rep in the graph. @param[in]  graph      Graph that will own the rep.  Must be non-NULL. @param[out] out_id     Receives the rep id of the new surface.  Must be non-NULL. @param[in]  plane      Plane data. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param plane] A [OcctlGeomPlane].
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_cylinder">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="cylinder" type="Ref<OcctlGeomCylindricalSurface>" />
			<description>
				Creates a cylindrical surface rep in the graph. @param[in]  graph     Graph that will own the rep.  Must be non-NULL. @param[out] out_id    Receives the rep id of the new surface.  Must be non-NULL. @param[in]  cylinder  Cylinder data.  @c radius must be > 0. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID @c radius is not positive. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param cylinder] A [OcctlGeomCylindricalSurface].
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_cone">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="cone" type="Ref<OcctlGeomConicalSurface>" />
			<description>
				Creates a conical surface rep in the graph. @param[in]  graph  Graph that will own the rep.  Must be non-NULL. @param[out] out_id Receives the rep id of the new surface.  Must be non-NULL. @param[in]  cone   Cone data.  @c semi_angle must be in (0, pi/2). @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID Invalid @c semi_angle. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param cone] A [OcctlGeomConicalSurface].
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_sphere">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="sphere" type="Ref<OcctlGeomSphericalSurface>" />
			<description>
				Creates a spherical surface rep in the graph. @param[in]  graph   Graph that will own the rep.  Must be non-NULL. @param[out] out_id  Receives the rep id of the new surface.  Must be non-NULL. @param[in]  sphere  Sphere data.  @c radius must be > 0. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID @c radius is not positive. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param sphere] A [OcctlGeomSphericalSurface].
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_torus">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="torus" type="Ref<OcctlGeomToroidalSurface>" />
			<description>
				Creates a toroidal surface rep in the graph. @param[in]  graph   Graph that will own the rep.  Must be non-NULL. @param[out] out_id  Receives the rep id of the new surface.  Must be non-NULL. @param[in]  torus   Torus data.  Both radii must be > 0 and major > minor. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL. @retval OCCTL_GEOMETRY_INVALID Invalid radii. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param torus] A [OcctlGeomToroidalSurface].
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="revolution_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlSurfaceRevolutionCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_surface_revolution_create_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_revolution">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="info" type="Ref<OcctlSurfaceRevolutionCreateInfo>" />
			<description>
				Creates a surface of revolution rep in the graph. @param[in]  graph  Graph that will own the rep.  Must be non-NULL. @param[out] out_id Receives the rep id.  Must be non-NULL. @param[in]  info   Versioned create-info.  Must be non-NULL.  The @c basis must be a valid curve rep id. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL, or @c info->basis is invalid. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the parameters. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param info] An optional [OcctlSurfaceRevolutionCreateInfo]. Pass null to use defaults.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="extrusion_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlSurfaceExtrusionCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_surface_extrusion_create_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_extrusion">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="info" type="Ref<OcctlSurfaceExtrusionCreateInfo>" />
			<description>
				Creates a surface of linear extrusion rep in the graph. @param[in]  graph  Graph that will own the rep.  Must be non-NULL. @param[out] out_id Receives the rep id.  Must be non-NULL. @param[in]  info   Versioned create-info.  Must be non-NULL.  The @c basis must be a valid curve rep id. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL, or @c info->basis is invalid. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID @c direction has zero length. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param info] An optional [OcctlSurfaceExtrusionCreateInfo]. Pass null to use defaults.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="rectangular_trimmed_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlSurfaceRectangularTrimmedCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_surface_rectangular_trimmed_create_info_t. Sets all fields to #OCCTL_SURFACE_RECTANGULAR_TRIMMED_CREATE_INFO_INIT. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_rectangular_trimmed">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="info" type="Ref<OcctlSurfaceRectangularTrimmedCreateInfo>" />
			<description>
				Creates a rectangular-trimmed surface rep in the graph. @param[in]  graph  Graph that will own the rep.  Must be non-NULL. @param[out] out_id Receives the rep id.  Must be non-NULL. @param[in]  info   Borrows it.  Versioned create-info.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL, or @c info->basis is invalid. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID Basis surface or trim parameters are invalid. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param info] An optional [OcctlSurfaceRectangularTrimmedCreateInfo]. Pass null to use defaults.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="offset_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlSurfaceOffsetCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_surface_offset_create_info_t. Sets all fields to #OCCTL_SURFACE_OFFSET_CREATE_INFO_INIT. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_surface_create_offset /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_offset">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="info" type="Ref<OcctlSurfaceOffsetCreateInfo>" />
			<description>
				Creates an offset surface rep in the graph. @param[in]  graph  Graph that will own the rep.  Must be non-NULL. @param[out] out_id Receives the rep id.  Must be non-NULL. @param[in]  info   Borrows it.  Versioned create-info.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL, or @c info->basis is invalid. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID Basis surface is invalid for offsetting. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param info] An optional [OcctlSurfaceOffsetCreateInfo]. Pass null to use defaults.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlSurfaceBsplineCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_surface_bspline_create_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_surface_create_bspline /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_bspline">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="info" type="Ref<OcctlSurfaceBsplineCreateInfo>" />
			<description>
				Creates a B-spline surface rep in the graph. @param[in]  graph  Graph that will own the rep.  Must be non-NULL. @param[out] out_id Receives the rep id.  Must be non-NULL. @param[in]  info   Versioned create-info.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL, required arrays are NULL, or periodic flags are not 0/1. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the definition. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param info] An optional [OcctlSurfaceBsplineCreateInfo]. Pass null to use defaults.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bezier_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlSurfaceBezierCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_surface_bezier_create_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_surface_create_bezier /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_bezier">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="info" type="Ref<OcctlSurfaceBezierCreateInfo>" />
			<description>
				Creates a Bezier surface rep in the graph. @param[in]  graph  Graph that will own the rep.  Must be non-NULL. @param[out] out_id Receives the rep id.  Must be non-NULL. @param[in]  info   Borrows it.  Versioned create-info.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the definition. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param info] An optional [OcctlSurfaceBezierCreateInfo]. Pass null to use defaults.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="create_bezier_grid">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="info" type="Ref<OcctlSurfaceBezierCreateInfo>" />
			<description>
				Creates a Bezier surface from a row-major pole grid. This is the grid-facing alias for #occtl_surface_create_bezier.  It uses the same create-info struct: @c poles are laid out as @c u * v_pole_count + v, and @c weights may be NULL for a non-rational Bezier surface. @param[in]  graph  Graph that will own the rep.  Must be non-NULL. @param[out] out_id Receives the rep id.  Must be non-NULL. @param[in]  info   Borrows it.  Versioned create-info.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL, or the pole grid is invalid. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID OCCT rejected the definition. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_surface_bezier_create_info_init, occtl_surface_create_bezier, occtl_surface_create_from_point_grid /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param info] An optional [OcctlSurfaceBezierCreateInfo]. Pass null to use defaults.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="reverse">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a new surface rep that is the U-reversed copy of @p surface_id. @param[in]  graph       Graph that owns both surfaces.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_id      Receives the rep id of the reversed surface.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL, or @p surface_id is invalid. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="transformed">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="transform" type="Ref<OcctlTransform>" />
			<argument index="3" name="out_id" type="int64_t" />
			<description>
				Creates a new surface rep by applying a 3D transform to @p surface_id. @param[in]  graph       Graph that will own the new rep.  Must be non-NULL. @param[in]  surface_id  Source surface rep id.  Must be valid. @param[in]  transform   Transform to apply. @param[out] out_id      Receives the rep id of the transformed surface.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL, or @p surface_id is invalid. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param surface_id] A occtl_rep_id_t.
				    [param transform] A [OcctlTransform].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="translated">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="delta" type="Ref<OcctlVector3>" />
			<argument index="3" name="out_id" type="int64_t" />
			<description>
				Creates a new surface rep by translating @p surface_id. @param[in]  graph       Graph that will own the new rep.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[in]  delta       Translation vector. @param[out] out_id      Receives the rep id of the translated surface.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL, or @p surface_id is invalid. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param surface_id] A occtl_rep_id_t.
				    [param delta] A [OcctlVector3].
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="rotated">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="axis" type="Ref<OcctlAxis1Placement>" />
			<argument index="3" name="angle" type="double" />
			<argument index="4" name="out_id" type="int64_t" />
			<description>
				Creates a new surface rep by rotating @p surface_id. @param[in]  graph       Graph that will own the new rep.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[in]  axis        Rotation axis. @param[in]  angle       Rotation angle in radians. @param[out] out_id      Receives the rep id of the rotated surface.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL, or @p surface_id is invalid. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param surface_id] A occtl_rep_id_t.
				    [param axis] A [OcctlAxis1Placement].
				    [param angle] Input parameter.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="scaled">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="origin" type="Ref<OcctlPoint3>" />
			<argument index="3" name="factor" type="double" />
			<argument index="4" name="out_id" type="int64_t" />
			<description>
				Creates a new surface rep by scaling @p surface_id about @p origin. @param[in]  graph       Graph that will own the new rep.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[in]  origin      Origin point for scaling. @param[in]  factor      Scale factor; must be non-zero. @param[out] out_id      Receives the rep id of the scaled surface.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_id is NULL, or @p surface_id is invalid. @retval OCCTL_GEOMETRY_INVALID @c factor is zero. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). /
				    [param graph] Input parameter.
				    [param surface_id] A occtl_rep_id_t.
				    [param origin] A [OcctlPoint3].
				    [param factor] Input parameter.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="area">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_area" type="PackedFloat64Array" />
			<description>
				Computes the surface area over the full bounded UV parameter domain. Unbounded surfaces, such as an infinite plane, cannot produce a finite area. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_area    Receives the surface area.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_area is NULL, or @p surface_id is invalid. @retval OCCTL_GEOMETRY_INVALID The surface is invalid or unbounded. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_area] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="project_point">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="point" type="Ref<OcctlPoint3>" />
			<argument index="3" name="out_u" type="PackedFloat64Array" />
			<argument index="4" name="out_v" type="PackedFloat64Array" />
			<argument index="5" name="out_distance" type="PackedFloat64Array" />
			<description>
				Projects a 3D point onto the surface and returns the closest UV coordinates. Uses OCCT's @c GeomAPI_ProjectPointOnSurf. @param[in]  graph         Graph containing the surface.  Must be non-NULL. @param[in]  surface_id    Surface rep id.  Must be valid. @param[in]  point         Point to project. @param[out] out_u         Receives the U parameter of the closest point.  Must be non-NULL. @param[out] out_v         Receives the V parameter of the closest point.  Must be non-NULL. @param[out] out_distance  Receives the shortest distance.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_u, or @p out_v is NULL, or @p surface_id is invalid. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param point] A [OcctlPoint3].
				    [param out_u] Output parameter.
				    [param out_v] Output parameter.
				    [param out_distance] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="uv_of_point">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="point" type="Ref<OcctlPoint3>" />
			<argument index="3" name="out_u" type="PackedFloat64Array" />
			<argument index="4" name="out_v" type="PackedFloat64Array" />
			<description>
				Returns the UV coordinates on the surface nearest to a given 3D point. Convenience function equivalent to #occtl_surface_project_point with @c out_distance = NULL. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[in]  point       Point to find UV for. @param[out] out_u       Receives the U parameter.  Must be non-NULL. @param[out] out_v       Receives the V parameter.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_u, or @p out_v is NULL, or @p surface_id is invalid. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param point] A [OcctlPoint3].
				    [param out_u] Output parameter.
				    [param out_v] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="kind">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_kind" type="int" />
			<description>
				Returns the kind of geometry of a surface rep. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_kind    Receives the surface kind.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_kind is NULL, or @p surface_id is invalid. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_kind] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="is_u_periodic">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_is_periodic" type="PackedInt32Array" />
			<description>
				Returns non-zero if the surface is periodic in the U direction. @param[in]  graph           Graph containing the surface.  Must be non-NULL. @param[in]  surface_id      Surface rep id.  Must be valid. @param[out] out_is_periodic Receives 1 if U-periodic, 0 otherwise.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_is_periodic is NULL, or @p surface_id is invalid. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_is_periodic] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="is_v_periodic">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_is_periodic" type="PackedInt32Array" />
			<description>
				Returns non-zero if the surface is periodic in the V direction. @param[in]  graph           Graph containing the surface.  Must be non-NULL. @param[in]  surface_id      Surface rep id.  Must be valid. @param[out] out_is_periodic Receives 1 if V-periodic, 0 otherwise.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_is_periodic is NULL, or @p surface_id is invalid. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_is_periodic] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="is_closed">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_is_closed" type="PackedInt32Array" />
			<description>
				Returns non-zero if the surface is closed in both U and V directions. Convenience over separate U and V closed checks. A surface is closed when IsUClosed() and IsVClosed() are both true. @param[in]  graph        Graph containing the surface.  Must be non-NULL. @param[in]  surface_id   Surface rep id.  Must be valid. @param[out] out_is_closed Receives 1 if closed in both U and V, 0 otherwise.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_is_closed is NULL, or @p surface_id is invalid. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_is_closed] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="is_periodic">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_is_periodic" type="PackedInt32Array" />
			<description>
				Returns non-zero if the surface is periodic in either U or V direction. Convenience over separate U and V periodic checks. A surface is periodic when IsUPeriodic() or IsVPeriodic() is true. @param[in]  graph           Graph containing the surface.  Must be non-NULL. @param[in]  surface_id      Surface rep id.  Must be valid. @param[out] out_is_periodic Receives 1 if periodic in either U or V, 0 otherwise.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_is_periodic is NULL, or @p surface_id is invalid. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_is_periodic] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="continuity">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_continuity" type="int" />
			<description>
				Returns the continuity class of the surface. @param[in]  graph           Graph containing the surface.  Must be non-NULL. @param[in]  surface_id      Surface rep id.  Must be valid. @param[out] out_continuity  Receives the continuity class.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_continuity is NULL, or @p surface_id is invalid. @threadsafe Yes. /
				    [param graph] Input parameter.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_continuity] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="parameter_range">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_u_min" type="PackedFloat64Array" />
			<argument index="3" name="out_u_max" type="PackedFloat64Array" />
			<argument index="4" name="out_v_min" type="PackedFloat64Array" />
			<argument index="5" name="out_v_max" type="PackedFloat64Array" />
			<description>
				Returns the parameter range of the surface. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_u_min   Start U parameter.  May be NULL. @param[out] out_u_max   End U parameter.  May be NULL. @param[out] out_v_min   Start V parameter.  May be NULL. @param[out] out_v_max   End V parameter.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL or @p surface_id is invalid. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_u_min] Output parameter.
				    [param out_u_max] Output parameter.
				    [param out_v_min] Output parameter.
				    [param out_v_max] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_plane">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_plane" type="Ref<OcctlGeomPlane>" />
			<description>
				Extracts plane data from a surface rep. @param[in]  graph      Graph containing the surface.  Must be non-NULL. @param[in]  surface_id Surface rep id.  Must be valid; kind must be @c OCCTL_SURFACE_KIND_PLANE. @param[out] out_plane  Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_plane is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a plane. @threadsafe Yes. @sa occtl_surface_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_plane] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_cylinder">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_cylinder" type="Ref<OcctlGeomCylindricalSurface>" />
			<description>
				Extracts cylindrical surface data. @param[in]  graph        Graph containing the surface.  Must be non-NULL. @param[in]  surface_id   Surface rep id.  Must be valid; kind must be @c OCCTL_SURFACE_KIND_CYLINDRICAL. @param[out] out_cylinder Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_cylinder is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a cylindrical surface. @threadsafe Yes. @sa occtl_surface_kind /
				    [param graph] Input parameter.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_cylinder] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_cone">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_cone" type="Ref<OcctlGeomConicalSurface>" />
			<description>
				Extracts conical surface data. @param[in]  graph      Graph containing the surface.  Must be non-NULL. @param[in]  surface_id Surface rep id.  Must be valid; kind must be @c OCCTL_SURFACE_KIND_CONICAL. @param[out] out_cone   Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_cone is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a conical surface. @threadsafe Yes. @sa occtl_surface_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_cone] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_sphere">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_sphere" type="Ref<OcctlGeomSphericalSurface>" />
			<description>
				Extracts spherical surface data. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid; kind must be @c OCCTL_SURFACE_KIND_SPHERICAL. @param[out] out_sphere  Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_sphere is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a spherical surface. @threadsafe Yes. @sa occtl_surface_kind /
				    [param graph] Input parameter.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_sphere] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_torus">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_torus" type="Ref<OcctlGeomToroidalSurface>" />
			<description>
				Extracts toroidal surface data. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid; kind must be @c OCCTL_SURFACE_KIND_TOROIDAL. @param[out] out_torus   Borrows it (caller-allocated slot).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_torus is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a toroidal surface. @threadsafe Yes. @sa occtl_surface_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_torus] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_revolution">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_axis" type="Ref<OcctlAxis1Placement>" />
			<description>
				Extracts the rotation axis of a surface of revolution. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid; kind must be @c OCCTL_SURFACE_KIND_REVOLUTION. @param[out] out_axis    Rotation axis.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a surface of revolution. @threadsafe Yes. @sa occtl_surface_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_axis] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_extrusion">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_direction" type="Ref<OcctlVector3>" />
			<description>
				Extracts the direction of a surface of linear extrusion. @param[in]  graph          Graph containing the surface.  Must be non-NULL. @param[in]  surface_id     Surface rep id.  Must be valid; kind must be @c OCCTL_SURFACE_KIND_EXTRUSION. @param[out] out_direction  Extrusion direction.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a surface of linear extrusion. @threadsafe Yes. @sa occtl_surface_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_direction] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_rectangular_trimmed">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_u_first" type="PackedFloat64Array" />
			<argument index="3" name="out_u_last" type="PackedFloat64Array" />
			<argument index="4" name="out_v_first" type="PackedFloat64Array" />
			<argument index="5" name="out_v_last" type="PackedFloat64Array" />
			<description>
				Extracts the parametric trim bounds of a rectangular-trimmed surface. @param[in]  graph        Graph containing the surface.  Must be non-NULL. @param[in]  surface_id   Surface rep id.  Must be valid; kind must be @c OCCTL_SURFACE_KIND_RECTANGULAR_TRIMMED. @param[out] out_u_first  First U trim bound.  May be NULL. @param[out] out_u_last   Last U trim bound.  May be NULL. @param[out] out_v_first  First V trim bound.  May be NULL. @param[out] out_v_last   Last V trim bound.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a rectangular-trimmed surface. @threadsafe Yes. @sa occtl_surface_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_u_first] Output parameter.
				    [param out_u_last] Output parameter.
				    [param out_v_first] Output parameter.
				    [param out_v_last] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="as_offset">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_offset" type="PackedFloat64Array" />
			<description>
				Extracts the signed offset distance of an offset surface. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid; kind must be @c OCCTL_SURFACE_KIND_OFFSET. @param[out] out_offset  Signed offset distance.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not an offset surface. @threadsafe Yes. @sa occtl_surface_kind /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_offset] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_u_degree">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_degree" type="PackedInt32Array" />
			<description>
				Returns the U degree of a B-spline surface. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_degree  Receives the U degree.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_degree is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a B-spline. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_degree] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_v_degree">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_degree" type="PackedInt32Array" />
			<description>
				Returns the V degree of a B-spline surface. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_degree  Receives the V degree.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_degree is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a B-spline. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_degree] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_u_pole_count">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_count" type="int" />
			<description>
				Returns the number of poles in the U direction of a B-spline surface. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_count   Receives the U pole count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a B-spline. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_v_pole_count">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_count" type="int" />
			<description>
				Returns the number of poles in the V direction of a B-spline surface. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_count   Receives the V pole count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a B-spline. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_is_rational">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_is_rational" type="PackedInt32Array" />
			<description>
				Returns non-zero if the B-spline surface is rational. @param[in]  graph         Graph containing the surface.  Must be non-NULL. @param[in]  surface_id    Surface rep id.  Must be valid. @param[out] out_is_rational  Receives 1 if rational; 0 otherwise.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_is_rational is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a B-spline. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_is_rational] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_u_knot_count">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_count" type="int" />
			<description>
				Returns the number of distinct U knot values of a B-spline surface. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_count   Receives the U knot count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a B-spline. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_v_knot_count">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_count" type="int" />
			<description>
				Returns the number of distinct V knot values of a B-spline surface. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_count   Receives the V knot count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a B-spline. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bezier_u_degree">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_degree" type="PackedInt32Array" />
			<description>
				Returns the U degree of a Bezier surface. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_degree  Receives the U degree.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_degree is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a Bezier. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_degree] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bezier_v_degree">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_degree" type="PackedInt32Array" />
			<description>
				Returns the V degree of a Bezier surface. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_degree  Receives the V degree.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_degree is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a Bezier. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_degree] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bezier_u_pole_count">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_count" type="int" />
			<description>
				Returns the number of poles in the U direction of a Bezier surface. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_count   Receives the U pole count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a Bezier. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bezier_v_pole_count">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_count" type="int" />
			<description>
				Returns the number of poles in the V direction of a Bezier surface. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_count   Receives the V pole count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a Bezier. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bezier_is_rational">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_is_rational" type="PackedInt32Array" />
			<description>
				Returns non-zero if the Bezier surface is rational (in U or V). @param[in]  graph         Graph containing the surface.  Must be non-NULL. @param[in]  surface_id    Surface rep id.  Must be valid. @param[out] out_is_rational  Receives 1 if rational; 0 otherwise.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_is_rational is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Surface is not a Bezier. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_is_rational] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_poles">
			<return type="Array[Ref[OcctlPoint3]]" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<description>
				Extracts the poles of a B-spline surface.  Two-call pattern. Poles are returned row-major: index = @c u * @c v_pole_count + @c v. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid; kind must be @c OCCTL_SURFACE_KIND_BSPLINE. @param[out] out_buf     May be NULL on sizing call. @param[in]  capacity    Elements @p out_buf can hold. @param[out] out_count   Required count (= u_pole_count * v_pole_count).  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [return] Array[Ref[OcctlPoint3]] result.
		</description>
		</method>
		<method name="bspline_u_knots">
			<return type="PackedFloat64Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<description>
				Extracts the distinct U knot values of a B-spline surface.  Two-call pattern. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_buf     May be NULL on sizing call. @param[in]  capacity    Elements @p out_buf can hold. @param[out] out_count   Required count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [return] Array of double values.
		</description>
		</method>
		<method name="bspline_v_knots">
			<return type="PackedFloat64Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<description>
				Extracts the distinct V knot values of a B-spline surface.  Two-call pattern. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_buf     May be NULL on sizing call. @param[in]  capacity    Elements @p out_buf can hold. @param[out] out_count   Required count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [return] Array of double values.
		</description>
		</method>
		<method name="bspline_u_multiplicities">
			<return type="PackedInt32Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<description>
				Extracts the U knot multiplicities of a B-spline surface.  Two-call pattern. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_buf     May be NULL on sizing call. @param[in]  capacity    Elements @p out_buf can hold. @param[out] out_count   Required count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes. /
				    [param graph] Input parameter.
				    [param surface_id] A occtl_rep_id_t.
				    [return] Array of int values.
		</description>
		</method>
		<method name="bspline_v_multiplicities">
			<return type="PackedInt32Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<description>
				Extracts the V knot multiplicities of a B-spline surface.  Two-call pattern. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_buf     May be NULL on sizing call. @param[in]  capacity    Elements @p out_buf can hold. @param[out] out_count   Required count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes. /
				    [param graph] Input parameter.
				    [param surface_id] A occtl_rep_id_t.
				    [return] Array of int values.
		</description>
		</method>
		<method name="bspline_weights">
			<return type="PackedFloat64Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<description>
				Extracts the per-pole weights of a rational B-spline surface.  Two-call pattern. Layout matches the poles array: row-major @c index = u * v_pole_count + v. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[out] out_buf     Output buffer, or NULL for sizing. @param[in]  capacity    Capacity of @p out_buf in doubles. @param[out] out_count   Required/filled double count.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Not a B-spline or surface is non-rational. @retval OCCTL_BUFFER_TOO_SMALL Buffer too small. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [return] Array of double values.
		</description>
		</method>
		<method name="bspline_u_flat_knots">
			<return type="PackedFloat64Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<description>
				Extracts the expanded (flat) U knot sequence of a B-spline surface.  Two-call pattern. The flat sequence repeats each distinct knot value by its multiplicity. Its length equals @c sum(u_multiplicities), which is the standard B-spline knot vector consumed by most numerical libraries. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid; kind must be @c OCCTL_SURFACE_KIND_BSPLINE. @param[out] out_buf     May be NULL on the sizing call. @param[in]  capacity    Number of @c double elements @p out_buf can hold. @param[out] out_count   Receives the required count.  Must be non-NULL. @retval OCCTL_OK               On success (including the sizing call). @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Handle is not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL @p capacity is smaller than @c *out_count. @threadsafe Yes. @sa occtl_surface_bspline_u_knots, occtl_surface_bspline_u_multiplicities, occtl_surface_bspline_v_flat_knots /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [return] Array of double values.
		</description>
		</method>
		<method name="bspline_v_flat_knots">
			<return type="PackedFloat64Array" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<description>
				Extracts the expanded (flat) V knot sequence of a B-spline surface.  Two-call pattern. The flat sequence repeats each distinct knot value by its multiplicity. Its length equals @c sum(v_multiplicities), which is the standard B-spline knot vector consumed by most numerical libraries. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid; kind must be @c OCCTL_SURFACE_KIND_BSPLINE. @param[out] out_buf     May be NULL on the sizing call. @param[in]  capacity    Number of @c double elements @p out_buf can hold. @param[out] out_count   Receives the required count.  Must be non-NULL. @retval OCCTL_OK               On success (including the sizing call). @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_count is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND       Handle is not a B-spline. @retval OCCTL_BUFFER_TOO_SMALL @p capacity is smaller than @c *out_count. @threadsafe Yes. @sa occtl_surface_bspline_v_knots, occtl_surface_bspline_v_multiplicities, occtl_surface_bspline_u_flat_knots /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [return] Array of double values.
		</description>
		</method>
		<method name="bspline_poles_view">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out_u_count" type="int" />
			<argument index="3" name="out_v_count" type="int" />
			<description>
				Zero-copy view of the poles array of a B-spline surface. Returns a direct pointer into the OCCT internal storage and the U / V pole counts.  Layout is row-major: @c index = u * v_pole_count + v.  Valid only while the surface rep is alive in the graph and no mutating call has been made on it. @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid; kind must be @c OCCTL_SURFACE_KIND_BSPLINE. @param[out] out_data    Receives a borrowed pointer to the pole array.  Must be non-NULL. @param[out] out_u_count    Receives the number of poles in U.  Must be non-NULL. @param[out] out_v_count    Receives the number of poles in V.  Must be non-NULL. @retval OCCTL_OK @retval OCCTL_INVALID_ARGUMENT  Any out-pointer is NULL, or @p surface_id is invalid. @retval OCCTL_WRONG_KIND        Surface is not a B-spline. @retval OCCTL_INTERNAL          OCCT's internal pole array is not contiguous row-major (does not occur with current OCCT; reported defensively if a future OCCT version changes the layout). @threadsafe Yes (read-only). /
				    [param graph] Input parameter.
				    [param surface_id] A occtl_rep_id_t.
				    [param out_u_count] Output parameter.
				    [param out_v_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="bspline_init">
			<argument index="0" name="out" type="Ref<OcctlSurfaceBspline>" />
			<description>
				Runtime initialiser for #occtl_surface_bspline_t. Sets @c struct_version and zeroes all other fields.  Use instead of the @c _INIT macro when C macros are not available (e.g. C# P/Invoke). @param[out] out  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_surface_as_bspline /
				    [param out] Output parameter.
		</description>
		</method>
		<method name="as_bspline">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="out" type="Ref<OcctlSurfaceBspline>" />
			<description>
				Fills a caller-allocated #occtl_surface_bspline_t with a complete read-only inspection view of the underlying B-spline surface. Pointers in @p out borrow from the surface rep and are valid while the rep exists in the graph. @param[in]     graph      Graph containing the surface.  Must be non-NULL. @param[in]     surface_id Surface rep id.  Must be valid; kind must be @c OCCTL_SURFACE_KIND_BSPLINE. @param[in,out] out        Borrows it.  Caller-allocated; must be non-NULL.  The caller sets @c struct_version on entry; the library fills the remaining fields on success and leaves them unchanged on failure. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out is NULL, or @p surface_id is invalid. @retval OCCTL_VERSION_MISMATCH @c out->struct_version is not supported. @retval OCCTL_WRONG_KIND       Surface is not a B-spline surface. @threadsafe Yes (read-only). @sa occtl_surface_bspline_init, occtl_surface_bspline_poles_view, occtl_surface_bspline_u_degree, occtl_surface_bspline_u_pole_count /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param out] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="eval_d0">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="v" type="double" />
			<argument index="4" name="out_point" type="Ref<OcctlPoint3>" />
			<description>
				Evaluates the surface at (u, v). @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[in]  u           U parameter. @param[in]  v           V parameter. @param[out] out_point   Receives the point on the surface.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL or @p surface_id is invalid. @retval OCCTL_OUT_OF_RANGE     (u, v) is outside the parameter domain. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param v] Input parameter.
				    [param out_point] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="eval_d1">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="v" type="double" />
			<argument index="4" name="out_point" type="Ref<OcctlPoint3>" />
			<argument index="5" name="out_d1u" type="Ref<OcctlVector3>" />
			<argument index="6" name="out_d1v" type="Ref<OcctlVector3>" />
			<description>
				Evaluates the surface and its first-order partial derivatives at (u, v). @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[in]  u           U parameter. @param[in]  v           V parameter. @param[out] out_point   Receives the point.  May be NULL. @param[out] out_d1u     Receives dS/du.  May be NULL. @param[out] out_d1v     Receives dS/dv.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL or @p surface_id is invalid. @retval OCCTL_OUT_OF_RANGE     Parameter is outside the domain. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param v] Input parameter.
				    [param out_point] Output parameter.
				    [param out_d1u] Output parameter.
				    [param out_d1v] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="eval_d2">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="v" type="double" />
			<argument index="4" name="out_point" type="Ref<OcctlPoint3>" />
			<argument index="5" name="out_d1u" type="Ref<OcctlVector3>" />
			<argument index="6" name="out_d1v" type="Ref<OcctlVector3>" />
			<argument index="7" name="out_d2u" type="Ref<OcctlVector3>" />
			<argument index="8" name="out_d2v" type="Ref<OcctlVector3>" />
			<argument index="9" name="out_d2uv" type="Ref<OcctlVector3>" />
			<description>
				Evaluates the surface and its first- and second-order partial derivatives at (u, v). @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[in]  u           U parameter. @param[in]  v           V parameter. @param[out] out_point   Receives the point.  May be NULL. @param[out] out_d1u     Receives dS/du.  May be NULL. @param[out] out_d1v     Receives dS/dv.  May be NULL. @param[out] out_d2u     Receives d2S/du2.  May be NULL. @param[out] out_d2v     Receives d2S/dv2.  May be NULL. @param[out] out_d2uv    Receives d2S/dudv.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL or @p surface_id is invalid. @retval OCCTL_OUT_OF_RANGE     Parameter is outside the domain. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param v] Input parameter.
				    [param out_point] Output parameter.
				    [param out_d1u] Output parameter.
				    [param out_d1v] Output parameter.
				    [param out_d2u] Output parameter.
				    [param out_d2v] Output parameter.
				    [param out_d2uv] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="eval_d3">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="v" type="double" />
			<argument index="4" name="out_point" type="Ref<OcctlPoint3>" />
			<argument index="5" name="out_d1u" type="Ref<OcctlVector3>" />
			<argument index="6" name="out_d1v" type="Ref<OcctlVector3>" />
			<argument index="7" name="out_d2u" type="Ref<OcctlVector3>" />
			<argument index="8" name="out_d2v" type="Ref<OcctlVector3>" />
			<argument index="9" name="out_d2uv" type="Ref<OcctlVector3>" />
			<argument index="10" name="out_d3u" type="Ref<OcctlVector3>" />
			<argument index="11" name="out_d3v" type="Ref<OcctlVector3>" />
			<argument index="12" name="out_d3uuv" type="Ref<OcctlVector3>" />
			<argument index="13" name="out_d3uvv" type="Ref<OcctlVector3>" />
			<description>
				Evaluates the surface and its first-, second-, and third-order partial derivatives at (u, v). @param[in]  graph       Graph containing the surface.  Must be non-NULL. @param[in]  surface_id  Surface rep id.  Must be valid. @param[in]  u           U parameter. @param[in]  v           V parameter. @param[out] out_point   Receives the point.  May be NULL. @param[out] out_d1u     Receives dS/du.  May be NULL. @param[out] out_d1v     Receives dS/dv.  May be NULL. @param[out] out_d2u     Receives d2S/du2.  May be NULL. @param[out] out_d2v     Receives d2S/dv2.  May be NULL. @param[out] out_d2uv    Receives d2S/dudv.  May be NULL. @param[out] out_d3u     Receives d3S/du3.  May be NULL. @param[out] out_d3v     Receives d3S/dv3.  May be NULL. @param[out] out_d3uuv   Receives d3S/du2dv.  May be NULL. @param[out] out_d3uvv   Receives d3S/dudv2.  May be NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph is NULL or @p surface_id is invalid. @retval OCCTL_OUT_OF_RANGE     Parameter is outside the domain. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param v] Input parameter.
				    [param out_point] Output parameter.
				    [param out_d1u] Output parameter.
				    [param out_d1v] Output parameter.
				    [param out_d2u] Output parameter.
				    [param out_d2v] Output parameter.
				    [param out_d2uv] Output parameter.
				    [param out_d3u] Output parameter.
				    [param out_d3v] Output parameter.
				    [param out_d3uuv] Output parameter.
				    [param out_d3uvv] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="eval_dn">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="u" type="double" />
			<argument index="3" name="v" type="double" />
			<argument index="4" name="nu" type="int" />
			<argument index="5" name="nv" type="int" />
			<argument index="6" name="out_derivative" type="Ref<OcctlVector3>" />
			<description>
				Returns a partial derivative vector at (u, v). @param[in]  graph           Graph containing the surface.  Must be non-NULL. @param[in]  surface_id      Surface rep id.  Must be valid. @param[in]  u               U parameter. @param[in]  v               V parameter. @param[in]  nu              Derivative order in U (>= 0). @param[in]  nv              Derivative order in V (>= 0). @param[out] out_derivative  Receives d^(nu+nv)S / du^nu dv^nv.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph or @p out_derivative is NULL, or @p surface_id is invalid. @retval OCCTL_OUT_OF_RANGE     Parameter is outside the domain. @threadsafe Yes. /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param u] Input parameter.
				    [param v] Input parameter.
				    [param nu] Input parameter.
				    [param nv] Input parameter.
				    [param out_derivative] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="interpolated_info_init">
			<argument index="0" name="info" type="Ref<OcctlSurfaceInterpolatedInfo>" />
			<description>
				Runtime initialiser for #occtl_surface_interpolated_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_surface_create_interpolated /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_interpolated">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlSurfaceInterpolatedInfo>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Creates a B-spline surface rep that interpolates (passes through) the given point grid. Uses OCCT's @c GeomAPI_PointsToBSplineSurface with uniform parameters. When @c is_u_periodic is set, the surface is made periodic in U. @param[in]  graph  Graph that will own the rep.  Must be non-NULL. @param[in]  info   Versioned create-info.  Must be non-NULL. @param[out] out_id Receives the rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL, required fields are NULL / too small, or @c is_u_periodic is not 0/1. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID Interpolation failed. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_surface_interpolated_info_init /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlSurfaceInterpolatedInfo]. Pass null to use defaults.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="approximated_info_init">
			<argument index="0" name="info" type="Ref<OcctlSurfaceApproximatedInfo>" />
			<description>
				Runtime initialiser for #occtl_surface_approximated_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_surface_create_approximated /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_approximated">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="info" type="Ref<OcctlSurfaceApproximatedInfo>" />
			<argument index="2" name="out_id" type="int64_t" />
			<description>
				Fits a B-spline surface rep to the given point grid within tolerance. Uses OCCT's @c GeomAPI_PointsToBSplineSurface (approximation mode). @param[in]  graph  Graph that will own the rep.  Must be non-NULL. @param[in]  info   Versioned create-info.  Must be non-NULL. @param[out] out_id Receives the rep id.  Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_id, or @p info is NULL, required fields are NULL / too small, degree range is invalid, or @c tolerance is negative. @retval OCCTL_VERSION_MISMATCH Unsupported @c struct_version. @retval OCCTL_GEOMETRY_INVALID Approximation failed. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe No (mutates graph). @sa occtl_surface_approximated_info_init /
				    [param graph] Input parameter.
				    [param info] An optional [OcctlSurfaceApproximatedInfo]. Pass null to use defaults.
				    [param out_id] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="point_grid_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlSurfacePointGridCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_surface_point_grid_create_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_surface_create_from_point_grid /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_from_point_grid">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="info" type="Ref<OcctlSurfacePointGridCreateInfo>" />
			<description>
				Creates a B-spline surface rep from a row-major point grid. This is the broad point-grid constructor. It delegates to OCCT's @c GeomAPI_PointsToBSplineSurface in approximation or interpolation mode. @param[in]  graph  Graph that will own the rep.  Must be non-NULL. @param[out] out_id Receives the rep id. Must be non-NULL. @param[in]  info   Borrows it. Must be non-NULL with a recognised @c struct_version. @retval OCCTL_OK                On success. @retval OCCTL_INVALID_ARGUMENT  @p graph, @p out_id, or @p info is NULL, @c p_next is non-NULL, the point grid is NULL / too small, the mode is unknown, degree range is invalid, or @c tolerance is negative. @retval OCCTL_VERSION_MISMATCH  @c struct_version is unrecognised. @retval OCCTL_GEOMETRY_INVALID  OCCT rejected the point grid. @retval OCCTL_OUT_OF_MEMORY     Allocation failed. @threadsafe No (mutates graph). @sa occtl_surface_point_grid_create_info_init, occtl_surface_create_interpolated, occtl_surface_create_approximated /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param info] An optional [OcctlSurfacePointGridCreateInfo]. Pass null to use defaults.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="boundary_curves_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlSurfaceBoundaryCurvesCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_surface_boundary_curves_create_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_surface_create_from_boundary_curves /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_from_boundary_curves">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="info" type="Ref<OcctlSurfaceBoundaryCurvesCreateInfo>" />
			<description>
				Creates a B-spline surface rep from contiguous boundary curves. Uses OCCT's @c GeomFill_BSplineCurves. The input curves must form a compatible boundary loop or strip accepted by OCCT. @param[in]  graph  Graph that will own the rep. Must be non-NULL. @param[out] out_id Receives the rep id. Must be non-NULL. @param[in]  info   Borrows it. Must be non-NULL with a recognised @c struct_version. @retval OCCTL_OK                On success. @retval OCCTL_INVALID_ARGUMENT  @p graph, @p out_id, or @p info is NULL, @c p_next is non-NULL, @c curves is NULL, @c curve_count is not 2, 3, or 4, an array entry is invalid, or @c style is unknown. @retval OCCTL_VERSION_MISMATCH  @c struct_version is unrecognised. @retval OCCTL_GEOMETRY_INVALID  OCCT rejected the boundary curves. @retval OCCTL_OUT_OF_MEMORY     Allocation failed. @threadsafe No (mutates graph). @sa occtl_surface_boundary_curves_create_info_init, occtl_surface_create_gordon /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param info] An optional [OcctlSurfaceBoundaryCurvesCreateInfo]. Pass null to use defaults.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="gordon_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlSurfaceGordonCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_surface_gordon_create_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_surface_create_gordon /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_gordon">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="info" type="Ref<OcctlSurfaceGordonCreateInfo>" />
			<description>
				Creates a Gordon B-spline surface rep from a profile/guide curve network. Uses OCCT's @c GeomFill_Gordon. The input curves must form a compatible intersecting network; non-rational curve networks are the OCCT-supported target for this constructor. @param[in]  graph  Graph that will own the rep. Must be non-NULL. @param[out] out_id Receives the rep id. Must be non-NULL. @param[in]  info   Borrows it. Must be non-NULL with a recognised @c struct_version. @retval OCCTL_OK                On success. @retval OCCTL_INVALID_ARGUMENT  @p graph, @p out_id, or @p info is NULL, @c p_next is non-NULL, a curve array is NULL / too small, an array entry is invalid, @c tolerance is non-positive, or @c parallel is not 0/1. @retval OCCTL_VERSION_MISMATCH  @c struct_version is unrecognised. @retval OCCTL_GEOMETRY_INVALID  OCCT rejected the curve network. @retval OCCTL_OUT_OF_MEMORY     Allocation failed. @threadsafe No (mutates graph). @sa occtl_surface_gordon_create_info_init, occtl_surface_create_interpolated /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param info] An optional [OcctlSurfaceGordonCreateInfo]. Pass null to use defaults.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="curve_grid_create_info_init">
			<argument index="0" name="info" type="Ref<OcctlSurfaceCurveGridCreateInfo>" />
			<description>
				Runtime initialiser for #occtl_surface_curve_grid_create_info_t. @param[out] info  Borrows it. NULL-tolerant; no-op when NULL. @threadsafe Yes. @sa occtl_surface_create_from_curve_grid /
				    [param info] Output parameter.
		</description>
		</method>
		<method name="create_from_curve_grid">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="out_id" type="int64_t" />
			<argument index="2" name="info" type="Ref<OcctlSurfaceCurveGridCreateInfo>" />
			<description>
				Creates a B-spline surface rep from an intersecting U/V curve grid. Uses OCCT's @c GeomFill_Gordon. The input curves must form a compatible network where each U-direction curve intersects each V-direction curve. @param[in]  graph  Graph that will own the rep. Must be non-NULL. @param[out] out_id Receives the rep id. Must be non-NULL. @param[in]  info   Borrows it. Must be non-NULL with a recognised @c struct_version. @retval OCCTL_OK                On success. @retval OCCTL_INVALID_ARGUMENT  @p graph, @p out_id, or @p info is NULL, @c p_next is non-NULL, a curve array is NULL / too small, an array entry is invalid, @c tolerance is non-positive, or @c parallel is not 0/1. @retval OCCTL_VERSION_MISMATCH  @c struct_version is unrecognised. @retval OCCTL_GEOMETRY_INVALID  OCCT rejected the curve network. @retval OCCTL_OUT_OF_MEMORY     Allocation failed. @threadsafe No (mutates graph). @sa occtl_surface_curve_grid_create_info_init, occtl_surface_create_gordon /
				    [param graph] Input parameter.
				    [param out_id] Output parameter.
				    [param info] An optional [OcctlSurfaceCurveGridCreateInfo]. Pass null to use defaults.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
		<method name="intersect_curve">
			<return type="Array[Ref[OcctlPoint3]]" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_id" type="int64_t" />
			<argument index="2" name="curve_id" type="int64_t" />
			<description>
				Computes intersection points between a curve rep and a surface rep. Uses OCCT's @c GeomAPI_IntCS.  Output uses the two-call buffer pattern: call with @p out_buf == NULL to learn the required count, allocate, then call again with a buffer of at least that size. @param[in]  graph      Graph containing both reps.  Must be non-NULL. @param[in]  curve_id   Curve rep id.  Must be valid. @param[in]  surface_id Surface rep id.  Must be valid. @param[out] out_buf    Buffer to receive intersection points. May be NULL on sizing call. @param[in]  capacity   Number of elements @p out_buf can hold. @param[out] out_count  Receives the number of intersection points. Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_count is NULL, or a rep id is invalid. @retval OCCTL_BUFFER_TOO_SMALL @p capacity < *@p out_count. @threadsafe Yes. @sa occtl_surface_surface_intersect /
				    [param graph] A [OcctlGraphHandle] handle.
				    [param surface_id] A occtl_rep_id_t.
				    [param curve_id] A occtl_rep_id_t.
				    [return] Array[Ref[OcctlPoint3]] result.
		</description>
		</method>
		<method name="surface_intersect">
			<return type="int" />
			<argument index="0" name="graph" type="Ref<OcctlGraphHandle>" />
			<argument index="1" name="surface_a" type="int64_t" />
			<argument index="2" name="surface_b" type="int64_t" />
			<argument index="3" name="tolerance" type="double" />
			<argument index="4" name="out_count" type="int" />
			<description>
				Computes intersection curves between two surfaces. Uses OCCT's @c GeomAPI_IntSS.  Returns an array of intersection rep ids. The array itself is allocated by the function; the caller must free it with @c free(). @param[in]  graph       Graph containing both surfaces.  Must be non-NULL. @param[in]  surface_a   First surface rep id.  Must be valid. @param[in]  surface_b   Second surface rep id.  Must be valid. @param[in]  tolerance   Intersection tolerance in 3D. @param[out] out_ids     Receives a caller-owned array of curve rep ids. Must be non-NULL.  Free with @c free(). @param[out] out_count   Receives the number of intersection curves. Must be non-NULL. @retval OCCTL_OK               On success. @retval OCCTL_INVALID_ARGUMENT @p graph, @p out_ids, or @p out_count is NULL, or a surface rep id is invalid. @retval OCCTL_OUT_OF_MEMORY    Allocation failed. @threadsafe Yes. @sa occtl_surface_intersect_curve /
				    [param graph] Input parameter.
				    [param surface_a] A occtl_rep_id_t.
				    [param surface_b] A occtl_rep_id_t.
				    [param tolerance] Input parameter.
				    [param out_count] Output parameter.
				    [return] Status code ([constant OcctlCore.OCCTL_OK] on success).
		</description>
		</method>
	</methods>
	<constants>
		<constant name="OCCTL_SURFACE_REVOLUTION_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_SURFACE_REVOLUTION_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_SURFACE_EXTRUSION_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_SURFACE_EXTRUSION_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_SURFACE_RECTANGULAR_TRIMMED_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_SURFACE_RECTANGULAR_TRIMMED_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_SURFACE_OFFSET_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_SURFACE_OFFSET_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_SURFACE_BSPLINE_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_SURFACE_BSPLINE_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_SURFACE_BEZIER_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_SURFACE_BEZIER_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_SURFACE_BSPLINE_VERSION_1" value="1u">
			Constant for #define OCCTL_SURFACE_BSPLINE_VERSION_1.
		</constant>
		<constant name="OCCTL_SURFACE_INTERPOLATED_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_SURFACE_INTERPOLATED_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_SURFACE_APPROXIMATED_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_SURFACE_APPROXIMATED_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_SURFACE_POINT_GRID_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_SURFACE_POINT_GRID_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_SURFACE_BOUNDARY_CURVES_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_SURFACE_BOUNDARY_CURVES_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_SURFACE_GORDON_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_SURFACE_GORDON_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_SURFACE_CURVE_GRID_CREATE_INFO_VERSION_1" value="1u">
			Constant for #define OCCTL_SURFACE_CURVE_GRID_CREATE_INFO_VERSION_1.
		</constant>
		<constant name="OCCTL_SURFACE_KIND_PLANE" value="0">
			Enum value = 0 Kind identifier. Defined in [enum OcctlSurfaces.occtl_surface_kind_t].
		</constant>
		<constant name="OCCTL_SURFACE_KIND_CYLINDRICAL" value="1">
			Enum value = 1 Kind identifier. Defined in [enum OcctlSurfaces.occtl_surface_kind_t].
		</constant>
		<constant name="OCCTL_SURFACE_KIND_CONICAL" value="2">
			Enum value = 2 Kind identifier. Defined in [enum OcctlSurfaces.occtl_surface_kind_t].
		</constant>
		<constant name="OCCTL_SURFACE_KIND_SPHERICAL" value="3">
			Enum value = 3 Kind identifier. Defined in [enum OcctlSurfaces.occtl_surface_kind_t].
		</constant>
		<constant name="OCCTL_SURFACE_KIND_TOROIDAL" value="4">
			Enum value = 4 Kind identifier. Defined in [enum OcctlSurfaces.occtl_surface_kind_t].
		</constant>
		<constant name="OCCTL_SURFACE_KIND_BSPLINE" value="5">
			Enum value = 5 Kind identifier. Defined in [enum OcctlSurfaces.occtl_surface_kind_t].
		</constant>
		<constant name="OCCTL_SURFACE_KIND_BEZIER" value="6">
			Enum value = 6 Kind identifier. Defined in [enum OcctlSurfaces.occtl_surface_kind_t].
		</constant>
		<constant name="OCCTL_SURFACE_KIND_REVOLUTION" value="7">
			Enum value = 7 Kind identifier. Defined in [enum OcctlSurfaces.occtl_surface_kind_t].
		</constant>
		<constant name="OCCTL_SURFACE_KIND_EXTRUSION" value="8">
			Enum value = 8 Kind identifier. Defined in [enum OcctlSurfaces.occtl_surface_kind_t].
		</constant>
		<constant name="OCCTL_SURFACE_KIND_RECTANGULAR_TRIMMED" value="9">
			Enum value = 9 Kind identifier. Defined in [enum OcctlSurfaces.occtl_surface_kind_t].
		</constant>
		<constant name="OCCTL_SURFACE_KIND_OFFSET" value="10">
			Enum value = 10 Kind identifier. Defined in [enum OcctlSurfaces.occtl_surface_kind_t].
		</constant>
		<constant name="OCCTL_SURFACE_KIND_UNDEFINED" value="11">
			Enum value = 11 Kind identifier. Defined in [enum OcctlSurfaces.occtl_surface_kind_t].
		</constant>
		<constant name="OCCTL_SURFACE_KIND_RESERVED_FUTURE" value="2147483647">
			Enum value = 2147483647 Kind identifier. Defined in [enum OcctlSurfaces.occtl_surface_kind_t].
		</constant>
		<constant name="OCCTL_SURFACE_POINT_GRID_MODE_APPROXIMATE" value="0">
			Enum value = 0 Defined in [enum OcctlSurfaces.occtl_surface_point_grid_mode_t].
		</constant>
		<constant name="OCCTL_SURFACE_POINT_GRID_MODE_INTERPOLATE" value="1">
			Enum value = 1 Defined in [enum OcctlSurfaces.occtl_surface_point_grid_mode_t].
		</constant>
		<constant name="OCCTL_SURFACE_POINT_GRID_MODE_RESERVED_FUTURE" value="2147483647">
			Enum value = 2147483647 Defined in [enum OcctlSurfaces.occtl_surface_point_grid_mode_t].
		</constant>
		<constant name="OCCTL_SURFACE_FILLING_STRETCH" value="0">
			Enum value = 0 Defined in [enum OcctlSurfaces.occtl_surface_filling_style_t].
		</constant>
		<constant name="OCCTL_SURFACE_FILLING_COONS" value="1">
			Enum value = 1 Defined in [enum OcctlSurfaces.occtl_surface_filling_style_t].
		</constant>
		<constant name="OCCTL_SURFACE_FILLING_CURVED" value="2">
			Enum value = 2 Defined in [enum OcctlSurfaces.occtl_surface_filling_style_t].
		</constant>
		<constant name="OCCTL_SURFACE_FILLING_RESERVED_FUTURE" value="2147483647">
			Enum value = 2147483647 Defined in [enum OcctlSurfaces.occtl_surface_filling_style_t].
		</constant>
	</constants>
</class>
