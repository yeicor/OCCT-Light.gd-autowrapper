[project]
name = "occt-light-gd-autowrapper"
version = "0.1.0"
description = "Deterministic godot-cpp bindings generator for OCCT-Light C headers"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "tree-sitter>=0.25.2",
    "tree-sitter-cpp>=0.23.4",
]
